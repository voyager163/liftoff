-- flight-log standard application schema
CREATE TABLE IF NOT EXISTS app_records (
  id integer GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  name varchar(255) NOT NULL,
  created_at timestamptz NOT NULL DEFAULT now()
);
