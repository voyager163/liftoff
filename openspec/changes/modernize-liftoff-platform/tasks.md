## 1. Baseline, traceability and release contracts

- [x] 1.1 Record the immutable v0.12.3 baseline and map all 27 capability deltas, issues #78-#82 and the operator Grafana dashboard to implementation and qualification owners; verify the traceability inventory has no unassigned requirement or deferred required executor.
- [x] 1.2 Define the exact existing supported project/component profiles, including truthful Vue-only adoption and unsupported-stack assessment-only outcomes; verify profile fixtures do not invent a backend, cloud target or historical generation.
- [x] 1.3 Implement the amended identity authorities from the design, including manifest 8, policy 8, credential-policy schema 2, activation/state/evidence/approval 4, graph 3, compatibility 5 and governance output 3; register the exact pre-amendment policy-7/credential-policy-schema-1 candidate and its preservation/transition readers before enabling the new writer, and verify independent protocol/update/repair identities and original records remain unchanged.
- [x] 1.4 Capture byte-exact fixtures for supported manifests v2-v7, historical activation v1/v2/v3, retained ancestor histories and sealed update/repair journals; verify the existing readers and recovery characterization tests establish the baseline without normalizing history.
- [ ] 1.5 Define the required macOS/Windows/Linux x64/arm64 and minimum-host/libc qualification matrix; verify every advertised artifact has an identified native runner and explicit host requirements rather than an inferred support label.
- [ ] 1.6 Prepare separate approval-bound qualification inputs for disposable GitHub/Azure and native-host resources, identities, permitted effects, spending, lifetime and cleanup limits, including any encrypted volumes, controlled keystore enrollment/unlock/key-read/restart/disposal or ACL changes; verify missing access is a named release blocker and no credentials or live side effects enter planning artifacts.
- [x] 1.7 Record the historical Windows failure evidence for run 34842047858/job 103969047717 without assuming a cause; verify each observed timeout, tool-identity and settlement symptom has a reproducible qualification case.

Minimum-host admission now requires separate authenticated native/minimum-host
reports for all six targets, measured OS/kernel/glibc values, exact registered
jobs and runner observations, and matching final signed artifact identities.
Copied policy floors and successful newer-host runs cannot qualify a minimum.
Task 1.5 remains open: approved minimum-host execution registrations and actual
platform/runtime qualification are still unavailable. Local regression evidence
does not establish vendor support, libstdc++/GLIBCXX floors or native execution.

Task 1.3 now implements the approved policy-8/schema-2 target. Twenty frozen
pure reader modules and the original graph preserve the exact pre-amendment
policy-7 candidate. Public reviewed update exercises the same-schema successor,
raw-byte preservation and stale-preview refusal; unsettled original operations
block migration rather than lose their checkpoints. The computed target graph
hash is `7ae2149bfe39b3983bd09c14f0b11ebb84f82ad276170f12cc2c250d780301e9`.
Original policies and approvals are never promoted into broader credential
authority. The frozen code remains inside the full production coverage scope.

## 2. Public protocol and shared execution kernel

- [x] 2.1 Introduce schema-1 public capability and new-command contracts with strict validation; verify unsupported schemas, fields, targets and capabilities fail before effects without relabeling existing command JSON bodies.
- [x] 2.2 Register the six capability owners with profile/platform support, required inputs, planner/executor/verifier/recovery, authorization mechanism, effect classes and qualification state; verify planner-only, prerequisite-blocked, implementation-missing and unqualified states remain distinct.
- [x] 2.3 Extract shared immutable-plan and input-binding primitives from existing update/repair paths; verify byte, mode, directory, target, tool, configuration and expiry changes invalidate the exact reviewed operation.
- [x] 2.4 Adapt existing update/repair execution and journal contracts behind the shared kernel without format conversion; verify sealed legacy recovery handles only original recorded effects and rejects unknown identities.
- [x] 2.5 Preserve command-specific authorization, including imperative owner-preserving upgrade and existing initialization flags, while retaining default-No exact-plan approval for reviewed project/provider/installation-migration work; verify no universal new fingerprint flag or blanket Yes authority is introduced.
- [x] 2.6 Implement one structured continuation contract retaining executable, argument array, cwd, project or installation target, scope, configuration reference/digest and required authority; verify every emitted action parses and preserves context after changing directories.
- [ ] 2.7 Preserve native shell rendering and canonical path boundaries across Windows, macOS and Linux; verify spaced paths, PowerShell metacharacters, UNC/drive paths, case/normalization collisions and links cannot redirect an operation.
- [x] 2.8 Unify operation-level outcome reporting without changing retained report identities; verify earlier approved effects, commit, verification, cleanup and remaining work cannot be collapsed into a success-shaped or untouched fallback.
- [x] 2.9 Retain cooperating locks and cross-command transaction exclusion; verify unfinished update, repair, adoption and private-workspace activity block conflicting new writers without discarding user changes.

Local continuation admission now rejects drive-relative Windows arguments,
incomplete UNC shares and device paths before cwd resolution can hide their
ambiguous identity. Ordinary relative project/input paths remain bound to the
recorded cwd. This closes a source gap in 2.7/9.5; their required native-host
execution evidence remains separate and unperformed.

## 3. Windows and cross-platform execution qualification

- [ ] 3.1 Reproduce the 0.12.3 Windows application-verification timeouts with recorded host, controller and tool conditions; verify the investigation distinguishes controller startup, command execution and settlement deadlines.
- [ ] 3.2 Diagnose and correct Windows npm/tool identity and environment-alias handling using actual supported executables; verify positive, incompatible-version, changed-file and malicious-shim cases without relaxing identity admission.
- [x] 3.3 Diagnose and correct controller launch, authentication and working-directory failures; preserve the existing workspace layout and recovery records, admit the resolved Windows cwd against the Win32 length bound before controller dispatch, and document explicit shorter storage selection for new work without relocation or approval reuse; verify real native startup, supported-length literal paths and bounded over-limit/pre-execution failures under stock supported PowerShell/.NET conditions without extending production deadlines.
- [x] 3.4 Qualify actual Win32 Job Object assignment, root exit, descendant accounting and cancellation; verify uncertain settlement never issues success or cleans a potentially active workspace.
- [ ] 3.5 Qualify restricted-host, AppLocker/WDAC, corrupted-asset and unsupported-controller failures; verify no policy bypass, undeclared helper download or fallback kill of unrelated processes occurs.
- [x] 3.6 Preserve POSIX owned-group settlement and private workspace recovery during the Windows correction; verify equivalent macOS/Linux success, timeout, interrupted-output and uncertain-owner cases.
- [ ] 3.7 Verify native helper assets and required host contracts from the installed artifact, not the checkout; verify supported relocation, read-only installation and argument/path handling on all required platforms.
- [ ] 3.8 Run the complete Windows repair/process boundary lane after the established fixes; verify prior failure cases pass without removing tests, manufacturing proof or merely increasing timeouts.

Authorized source CI runs `35331613734` and `35334575245` exercised the
validation branch without deployment, signing or publication. Full Git history
is required for immutable historical readers, and packaged asset checkout bytes
are now LF-stable on Windows rather than normalized after integrity checks.
Windows boundary failures fell from 484 to 38; the second run still failed and
the aggregate coverage/macOS/Linux jobs exhausted their existing job budgets.
Neither run qualifies the release. Remaining investigation includes native
long-working-directory admission and Windows inspection/repair timeouts.
Renderer regressions now cover native paths independently of the inspecting
host, and repair test cleanup uses exact bigint filesystem identities instead
of rounded Windows inode numbers. These corrections do not close native
qualification tasks or authorize cleanup after uncertain process settlement.

Diagnostic run `35339878937` passed the preview regressions but still hit native
controller test deadlines. Follow-up `35340320575` kept the production deadlines
unchanged and allowed the newly added paired diagnostic to report both attempts:
333-character canonical and 337-character namespace cwd forms each returned
`CreateProcessW` error 267. Shorter cwd attempts instead exited 134 with settled
process trees, and total controller attempts took approximately 23-26 seconds.
The short-path child failure and startup cost therefore require separate
diagnosis. The approved refinement is explicit over-limit admission and shorter
storage guidance, not a new workspace format or a claim of native success.
At that point, tasks 3.1-3.8 remained open.

The approved cwd limit now rejects over-limit execution before controller or
target dispatch, preserves cancellation precedence and uses fixed safe remedies
for verification and preparation. Real Windows runs `35351663348` and
`35352074826` passed the focused source lane, including 180/258-code-unit literal
cwd execution, 259/320-code-unit refusal, unchanged original workspace records
and original-storage recovery, and native nonce/missing-directory rejection.
The short-path fixture needed the Windows `SystemRoot` prerequisite rather than
an empty target environment; target credentials and unrelated ambient variables
are still not inherited.

Instrumentation separated approximately 22 seconds of `Add-Type` command
discovery from approximately 0.1 seconds of compilation. Loading the stock
Utility module by its exact `PSHOME` path and disabling unrelated controller
module auto-discovery reduced the measured ready frame to approximately 0.5
seconds; actual short/boundary command cases completed in approximately 0.8
seconds. The pinned controller and resource catalog digests were regenerated.
No production deadline, execution policy, Job Object requirement or workspace
format was relaxed. Full source matrix run `35352071630` is separate and still
pending; these focused results do not qualify installed artifacts or all hosts.

Task 3.3 is complete for the exercised stock Windows source-host lane, including
the final cancellation/deadline-reporting regression run `35352572336` at
`58464a5f65ecc65fcc0ec56924db1397f6a5669d`. Minimum-host/architecture coverage,
installed-artifact qualification, host restrictions and the complete boundary
matrix remain separate open tasks. Native descendant, timeout, output-limit,
neighbor-preservation and cancellation fixtures are being exercised separately
for task 3.4; mocked protocol results alone do not complete it.

Task 3.4 now has passing native source-host evidence in run `35353355859` at
`36e14f5584bdade479bbb188fe14fd630f29cd52`: the inherited Job tracks a detached
descendant after the Node root exits, timeout and output-limit termination
settle the owned tree while an unrelated neighbor remains alive, and
cancellation after an actual descendant starts retains an uncertain outcome
and its fixture instead of granting cleanup. The original non-detached
root-exit fixture was corrected because Node's own child-job teardown
terminated that child first; no controller guarantee was weakened to pass it.
This completes the exercised native Job Object behavior, not minimum-host,
architecture-wide or installed-artifact qualification.

Task 3.6 has passing native POSIX source-host evidence from run `35352071630`
at `d9783d087a905ccdb361b6b7bbb5d6f69ac48bcf`. Both Ubuntu and macOS passed
all applicable `process-runner`, `state-migration-process` and
`repair-workspaces` cases: 83 passed per host, with only the two Windows-specific
cases unselected. They cover actual owned groups, bounded interruption/output,
settlement and guarded workspace recovery. The overall source run still failed
in other suites; these scoped passes do not close the full-matrix, encrypted
custody or installed-artifact gates.

## 4. Six-engine module boundaries

- [x] 4.1 Introduce the six application ownership modules and explicit composition root while keeping CLI parsing/presentation thin; verify public help, dispatch and capability ownership remain coherent.
- [x] 4.2 Separate governance inspection, planning, approval, execution, verification and presentation from the large command coordinator; verify characterization tests preserve supported behavior except the explicitly changed contracts.
- [x] 4.3 Split activation validation into coherent contract families sharing canonical identity/confinement rules; verify malformed, mixed, future and historical fixtures retain fail-closed behavior.
- [x] 4.4 Separate canonical policy/skill rendering from metadata assembly and compatibility selection; verify generated output changes only for approved identities, templates and instructions.
- [x] 4.5 Keep existing update/repair feature coordinators as composition over reusable submodules rather than parallel engines; verify application effects still use the released transaction and verification machinery.
- [x] 4.6 Extend import-boundary and cycle tests for the new ownership map and public protocol; verify pure domain code has no I/O dependency and no new code routes through retired compatibility facades.

## 5. Profiles, packaged resources and composable templates

- [x] 5.1 Define schema-1 standards/profile and resource catalogs with stable IDs, revisions, digests, component boundaries and actual evaluation coverage; verify duplicate, unknown and incompatible entries fail validation.
- [x] 5.2 Create the explicit template catalog and reusable common/backend/GenAI/frontend/infrastructure/workflow components; verify selected compositions reuse canonical resources rather than duplicated whole starters.
- [x] 5.3 Move suitable static content into packaged template assets while retaining typed conditional generators; verify representative and double-render fixtures preserve bytes and logical IDs except intentional changes.
- [x] 5.4 Bind governance, skills, locks and supported-stack entries through exact declared resource identities; verify a missing/damaged dependency blocks generation without fetching mutable replacements.
- [x] 5.5 Enforce exact output ownership, collision and retirement inventories across composed components; verify duplicate destinations, Windows aliases, unsafe links and unregistered deletion candidates fail before writes.
- [x] 5.6 Preserve all existing supported API/GenAI pattern and OpenSpec/Spec Kit combinations, maturity labels and retired Power Apps rejection; verify the generation matrix does not infer unsupported specialization or restore retired output.
- [x] 5.7 Introduce relocatable build-info/resource access independent of cwd and source layout; verify generation, help/version and resource-consuming operations work from read-only installed bundles outside the checkout.

Resource/runtime closure is verified for the darwin-arm64 development bundle,
including real Node/Fastify and RAG plans, packaged rendering, staged writes,
and owning manifest readback. Independent relocation used an empty PATH and a
read-only payload with literal quote/metacharacter paths. This does not qualify
signed releases, owner channels, completed initialization/framework execution,
or other native hosts; those remain separately gated below.

## 6. Whole-project standards assessment

- [x] 6.1 Implement explicit project/repository/component boundary selection for `assess`, including directories without Git or Liftoff metadata; verify no initialization, provider access, script execution or receipt creation occurs.
- [x] 6.2 Build bounded source, declaration, lock, test, container, workflow, infrastructure, framework and reference inventory with protected payload exclusions; verify partial observations remain unknown rather than absent or aligned.
- [x] 6.3 Resolve supported profile targets from explicit selection and evidence, keeping conflicting or unsupported stacks diagnostic-only; verify comments and filename resemblance do not establish framework support.
- [x] 6.4 Implement evidence-backed findings and coverage classification against the installed profile/catalog identity; verify custom source can satisfy a rule without starter-byte equality and no model claim becomes proof.
- [x] 6.5 Expose deterministic schema-1 assessment results with scope-specific 0/1/2 outcomes; verify changed inputs, missing coverage, unsupported profiles and invalid catalogs produce the declared distinct results.
- [x] 6.6 Route findings to real context-bound update/adopt/repair/governance/install capabilities without authorizing them; verify all recommendations preserve project, cwd and configuration binding and contain no secret payloads.

## 7. Manifest 8 and reviewed in-place adoption

- [x] 7.1 Implement strict generated/adopted/component-only manifest-8 forms and retain isolated supported v2-v7 readers; verify historical uncertainty and actual profile facts are preserved without fake generation hashes.
- [x] 7.2 Extend validation, doctor, update, rendering and governance applicability consumers for truthful adopted component-only profiles; verify Vue-only sources do not acquire invented backend, pattern or cloud identities.
- [x] 7.3 Implement reviewed v2-v7-to-v8 metadata migration with exact ownership release and original provenance preservation; verify check is non-mutating and intentionally changed/deleted application files are not restored.
- [x] 7.4 Implement adoption planning from current observations without requiring or prewriting a real Liftoff manifest; verify explicit adoption context supplies reusable inspection/verification safely and no prerequisite is bypassed with fabricated history.
- [x] 7.5 Implement exact adoption source/destination mappings, declared additions, reference review and absence/byte/mode preconditions; verify ambiguous moves, unsupported conversions and occupied destinations remain blocked.
- [x] 7.6 Reuse existing private candidate, locked preparation, process supervision and staged check primitives for adoption application effects; verify the original application-patch contract does not gain arbitrary addition or protected-metadata authority.
- [x] 7.7 Implement the separate deterministic adoption metadata/framework/integration producer with official staged framework support; verify each addition has exact ownership and user/framework collisions are preserved.
- [x] 7.8 Implement default-No interactive and exact non-TTY adoption approval across preparation, scripts, network and file/metadata effects; verify late cancellation reports earlier effects and no unapproved transaction runs.
- [x] 7.9 Implement schema-1 adoption records, guarded commit, readback and attributable recovery; verify interruption, changed destinations, retained backups and repeated matching adoption do not corrupt history.
- [x] 7.10 Preserve `migrate` as source-read-only fresh-target generation using manifest 8 and complete planning artifacts; verify non-empty-target refusal, explicit target overrides and unsupported semantic conversion remain distinct from in-place adoption and installation handover.

## 8. Existing-project maintenance and generator defects

- [x] 8.1 Integrate current profiles, manifest migration and skill catalogs into managed update without widening production-file comparison; verify adopted custom layouts, skipped conflicts and exact retirements retain their existing authority rules.
- [x] 8.2 Preserve repair contract 1, unchanged recipe/record semantics, historical seals and separate private-workspace recovery after extraction; verify native CLI replacement alone changes no project or journal.
- [x] 8.3 Add the four explicit Azure TLS/private-blob defaults for #80 in every affected generated profile; verify exact resource values and preservation of unrelated settings with generator tests.
- [x] 8.4 Add `azure-baseline-settings` version-1 inspection and planning for supported existing HCL; verify already-current layout, stronger compliant values and ambiguous/custom expressions have correct no-op or blocked outcomes.
- [x] 8.5 Implement baseline-setting validation, exact approved configuration/history writes and recovery outside the application-patch lane; verify no cloud/state effects occur and application patches still reject infrastructure.
- [x] 8.6 Correct Go and Node Scalar/schema routing for #81 with prefix-safe references and relative canonicalization; verify canonical/trailing-slash direct and proxied URLs retain queries and do not trust forwarded-host headers.
- [x] 8.7 Qualify standard Python and supported GenAI documentation against the same routing contract; verify actual JSON media type and canonical schema paths/components equality, including rejection of SPA HTML with HTTP 200.
- [x] 8.8 Exercise reviewed existing-handler remediation with customized business endpoints and references; verify only mapped code changes after matching staged checks and ordinary update does not overwrite handlers.

## 9. Activation 4, input binding and publication recovery

- [x] 9.1 Register frozen historical v1/v2/v3 tuples/algorithms and the new activation family with computed graph/phase digests; verify unknown combinations and edited compatibility metadata cannot authorize execution.
- [x] 9.2 Introduce per-phase consumed-input projection and canonical absent-input handling; verify later Azure-only inputs do not alter committed/pushed identity while real payload, repository or destination changes still invalidate proof.
- [x] 9.3 Validate explicit non-placeholder Azure subscription/tenant/region inputs before provider execution; verify missing or nil bindings produce non-executable input guidance rather than a zero-UUID operation.
- [x] 9.4 Verify the explicitly requested Azure account identity and usable state rather than ambient defaults; verify mismatching account/tenant observations never issue success evidence.
- [ ] 9.5 Preserve normalized inputs/configuration bindings across status, plan, verify, approve, apply and resume; verify actions parse and retain the original target after changing cwd on Windows/macOS/Linux.
- [x] 9.6 Implement schema-3 governance output with explicit local/repository/activation/lifecycle progress and correct verify exits; verify exactly 0 for consistent-complete, 2 for consistent-incomplete and 1 for inconsistency while unscoped calls still mean activation.
- [x] 9.7 Extend normal update preview and successor transactions to exact v1/v2/v3-to-v4 lanes; verify byte-preserving history, manifest 8, ancestry, concurrent-change refusal and committed-but-incomplete revalidation.
- [x] 9.8 Implement the reviewed affected-v3 publication readback/revalidation path for #78; verify actual unchanged local/remote publication resumes with later Azure inputs without another commit/push or rewritten old receipts.
- [x] 9.9 Distinguish verified published commits from later unpushed metadata/workflow changes; verify real changed publication blocks and later new content needs independent publication authority.
- [x] 9.10 Align doctor, governance assessment, status, resume and verification with current v4 and preserved v1-v3 history; verify no consumer treats retained history as current proof or repository success as full activation.
- [x] 9.11 Preserve retention/disposal obligations and unsupported sensitive-state boundaries through identity migration; verify original due times, disposed material and private references are neither reset nor recreated.

Affected-v3 publication source coverage includes real fixture-owned local Git,
records emitted by the immutable released serializers, the actual reviewed
successor and current public approval/apply paths, and selected private-store
authority. GitHub and external tool observations remain explicitly bounded test
doubles; the separate live end-to-end qualification in 15.10 is not established.

## 10. Repository-only governance and ruleset compatibility for issues 79 and 82

- [x] 10.1 Add dedicated repository phases and explicit shared local/publication prerequisites to the canonical graph; verify repository scope has no Azure/production dependency and default unscoped governance remains activation.
- [x] 10.2 Implement #82's supported pull-request field decoding for dismissal restrictions, extra approval and required reviewers, including nested documented shapes; verify defaults together/individually, older omission, meaningful values and malformed/unknown fields with sanitized provider fixtures.
- [x] 10.3 Update effective review-policy predicates without discarding observed configuration; verify a true extra-approval flag is neutral at zero approvals while positive conditional reviewer requirements and enabled restrictions retain their actual meaning.
- [x] 10.4 Qualify normalization consistency across assessment, planning and supported readback; verify neutral default additions cause no unnecessary write, genuinely unknown enforcement stays blocked and historical observation/digest meanings are not silently rewritten.
- [x] 10.5 Implement repository identity/ref/control/capability discovery using bounded authorized GitHub observations; verify no Azure configuration or credential access is required for repository-only work.
- [x] 10.6 Implement source-workflow publication through allowed GitFlow operations with exact source/actor/ref binding; verify protected branches are changed only through permitted PR/check paths without branch bypasses.
- [x] 10.7 Implement actual positive and controlled-negative source-check qualification on reviewed unmerged fixtures; verify exact workflow/job/context identity and reject synthetic statuses or infrastructure failure as negative proof.
- [x] 10.8 Implement the real owned GitHub settings/ruleset planner and executor with actor resolution; verify exact payload approval, stale-read refusal, foreign-control preservation and per-write readback.
- [x] 10.9 Implement repeat reconciliation as a zero-write no-op for matching controls; verify idempotent readback and partial-failure records without automatic protection removal.
- [x] 10.10 Implement the explicitly approved main-update hold and baseline binding; verify no production release/tag, fabricated staging result, direct main push or branch bypass is created.
- [x] 10.11 Implement separately reviewed hold replacement after real applicable qualification; verify missing proof, changed main tip, changed owned controls and stale approval retain the hold.
- [x] 10.12 Qualify independent repository completion and later full-activation continuation; verify repository proof cannot satisfy production, state-retention or full-enforcement evidence contracts.

Source-check implementation now requires the exact bound job's native assertion
report and single-failure summary. Generic failed steps, check summaries and
archived fixture source alone cannot qualify deliberate failure. Source
regressions include actual local Node assertion execution and bounded provider
fixtures; live GitHub qualification in 15.8 remains separate and unperformed.

Hold replacement now consumes the distinct full-activation qualification and
original source-check artifact custody through the production readers. Local
regressions reject missing/pending proof, expired approval and main/control
drift while preserving the hold. Public selected-scope regressions now establish
repository-only completion and subsequent activation continuation without
borrowing production or retention proof. Tasks 10.11/10.12 describe source
implementation and local acceptance, not a performed live hold replacement.

## 11. Complete the production executor scope of issue 78

- [x] 11.1 Complete real workflow-source producers for bootstrap and application workflows using the qualified GitHub publication primitives; verify exact source/run/artifact readback instead of file-presence success.
- [ ] 11.2 Implement policy-8/credential-policy-schema-2 private credential enrollment and independent usage/permission readback with PAT/App equivalence, exact actual provider grants, broader organization/billing/Actions-read disclosure and fresh plan-bound approval; preserve schema-1 policies, ownership and approvals through exact reviewed transitions, reject stale or extra/missing grants, keep intended operations narrower than provider reach, and verify protected values never enter chat, argv, public records or generated files. Retain independent PAT bearer/lifetime proof and supported create-only secret semantics without inferred replacement authority.
- [x] 11.3 Implement provider readiness and any separately approved registration derived from exact planned resource types; verify all required namespaces, failure classification and retained shared registrations.
- [ ] 11.4 Implement explicit state-path selection and existing-private-path verification through registered macOS, Linux and Windows adapters; verify ownership, encrypted custody, reachability, locking/versioning and required sensitive-read authority without publishing state payloads.
- [ ] 11.5 Implement the bounded approved bootstrap-local producer and its durable checkpoints across the registered native private-state hosts; verify only access-establishing scope occurs before dependent provisioning and no fake ready result is returned.
- [x] 11.6 Implement supported repository-dedicated private runner provisioning/assignment and network validation; verify applicable routing, DNS, egress and target reachability and block unsupported host/account combinations.
- [ ] 11.7 Implement private-backend proof and remote-import verification through the registered macOS, Linux and Windows state adapters; verify exact mappings, protected transport, platform-correct locks, fresh no-change proof and absence of unapproved resource replacement.
- [x] 11.8 Complete application-prerequisite and immutable artifact producers; verify actual registry/identity prerequisites, source-bound builds and digest readback rather than placeholder image evidence.
- [ ] 11.9 Implement the application-foundation producer using the exact approved infrastructure plan and selected native private-state adapter on macOS, Linux and Windows; verify real resource identity/health readback, protected process I/O and checkpointed partial effects.
- [x] 11.10 Implement dev qualification with actual applicable runtime/workflow evidence; verify source, artifact, environment and principal bindings and reject skipped or stale proof.
- [x] 11.11 Implement staging qualification with actual applicable private access and checks; verify missing runner/permission/input is an explicit prerequisite, not fabricated completion.
- [x] 11.12 Implement production rehearsal and full-activation green/red proof for profiles that require them; verify environment applicability, explicit cost/effect approval and no synthetic production substitute.
- [x] 11.13 Complete full-activation ruleset application and live readback using the real shared GitHub adapter; verify separate final authority, exact qualification and correct interaction with repository-only holds.
- [ ] 11.14 Qualify existing built-in publication, approval, remote-ready and retained-state-disposal producers under the new identities and expanded native custody providers; verify original macOS records/keys remain preserved and no producer regresses or acquires blanket scope from another provider, host or phase.
- [ ] 11.15 Implement bounded pending-operation polling and registered recovery for each external-effect producer; verify actual operation IDs, no duplicate dispatch, current preconditions and no claim of cross-provider atomic rollback.
- [x] 11.16 Keep every required producer unqualified until its actual provider/host/recipe evidence passes the release gate; verify an injected-only adapter or removal of a blocker flag cannot qualify the coordinated release.
- [ ] 11.17 Audit and register exact native private-state storage, key, principal, tool, lock/write and helper contracts for Linux and Windows, including x64/arm64 and minimum-host prerequisites; verify actual supported interfaces and pinned OpenTofu source semantics before enabling a new writer, and preserve existing macOS identities without a guessed global schema bump.
- [ ] 11.18 Implement the registered Linux private-state adapters for observed encrypted local storage, private ownership/access, controlled enrollment/restart-qualified key custody, exact POSIX locking/write semantics and owned-process/private-I/O lifecycle; verify unsafe storage, stale keys, changed identity, lock contention and uncertain settlement fail closed without plaintext or ambient-credential fallback.
- [ ] 11.19 Implement the registered Windows private-state adapters for observed encrypted storage, SID/DACL ownership, native key custody, actual Windows OpenTofu locking/write semantics and owned Job Object execution with protected I/O; verify no private payload reaches ordinary temporary logs or argv and unsupported policy/host conditions fail without bypass or timeout increases.
- [ ] 11.20 Integrate native adapter selection across all private-state producers, consumers, checkpoints and recovery, with explicit identities/readers for any new record semantics; verify original macOS bytes, keys, receipts and approvals are preserved, stale/foreign host authority rejects, and cross-host move/rekey/conversion is never implicit.
- [ ] 11.21 After separate bounded host-fixture approval, qualify each required native private-state host/architecture combination with real encrypted-storage, key, lock, private-I/O, cancellation, descendant, no-op and recovery cases, including controlled Linux enrollment and fresh-process persisted-key recovery; verify portable fixtures are labeled separately, unavailable prerequisites remain named blockers and no cloud effects or release authority are inferred.
- [ ] 11.22 Audit and register the exact supported controlled-GNOME daemon, dependency, IPC, creation/unlock, persisted-format and durability interfaces, plus independent enrollment/key-reference/recovery identities; verify authoritative backing-object and encrypted-generation binding without desktop-daemon assumptions, internal unsupported APIs or password-derived public verifiers before enabling enrollment.
- [ ] 11.23 Implement separately selected default-No/exact-plan Linux keystore enrollment with absence-bound private scope, protected operator input, pre-effect checkpoints, actual owned-process and returned-object custody, and private durable readback; verify occupied/stale targets, canceled input, plaintext/empty-password fallback and ambiguous outcomes cannot create a ready key or authorize ordinary desktop-keyring changes.
- [ ] 11.24 Implement fresh-process key-bound restart verification, interrupted enrollment recovery, later use admission and separately approved owned-store disposal; verify no cached-key substitute, duplicate creation, password reset, implicit rekey/cross-host conversion or unsafe deletion, and preserve all preexisting macOS/external-key records and dependent retained artifacts.

The full cross-platform source matrix exposed a real macOS-only private-state
foundation, not merely host-dependent fixture metadata. The user approved
extending native support to Linux and Windows rather than accepting mocked
success or retaining macOS-only execution as the coordinated endpoint.
Tasks 11.4, 11.5, 11.7, 11.9 and 11.14 are reopened for this expanded host scope;
their completed macOS implementation and historical evidence remain preserved.
Tasks 11.17-11.21 own the new native contracts, adapters and integration.
Implementation/source-CI approval does not authorize encrypted-volume
provisioning, ACL/policy changes, real key-store access or cloud qualification.

The original native POSIX protocol tuple is now held in a pure domain module
and re-exported through its original adapter API. Its canonical digest remains
`fa280a11fdddfacf4d868f6d3275fd2e04630496a1dc31aba5bc75f3a81fc640`;
regressions pin every original field and the unchanged existing behavior.
This is preservation groundwork for 11.17, not Linux/Windows registration or
permission to enable either writer before its native contracts are audited.

The bounded source audit has now registered distinct POSIX and Windows lock
facts, including Windows lock blob
`676e1318c25b9d32aa50e86dc8edfcb204bab3f4`, exact byte-range/handle lifetime and
the pinned state manager's non-propagated sync errors. This is explicitly
source-only evidence. Task 11.17 remains open: Linux fscrypt-v2 is a per-object
storage candidate, not a whole-volume assertion; generic Secret Service does
not prove durable protected key custody. Windows BitLocker observation requires
authorized administrator/packet-private WMI access, and Credential Manager
description itself reads protected bytes. These remaining contracts must be
resolved without plaintext fallback, inferred authorization or mocked native
qualification. No new platform writer, key access or privileged host effect
has been enabled.

The user-requested password-protected GNOME Keyring audit is complete but does
not close Linux key custody. Its supported read-only interfaces cannot bind an
exact live key to a durably saved encrypted collection. The GNOME-specific
`CKA_TRUSTED` attribute observes a nonempty loaded password, not the backing
file or persisted generation; encrypted Secret Service transport does not fill
that gap. Pinned source identities, evidence and the two persistence-proof
blockers are recorded in `docs/private-state-activation.md`. The user has now
approved controlled-store enrollment and restart recovery as implementation
scope in decision 13c and tasks 11.22-11.24. Real daemon/store/key operations
remain separately unauthorized; this planning approval does not establish
persistence proof. Task 11.17 and the dependent Linux writer remain open.

Independent Linux POSIX tool/lock exports and a distinct schema-2 synthetic
native-lock qualification entrypoint are implemented as groundwork. Existing
Darwin script bytes, protocol/qualification digests and actual-qualification
authority remain preserved. Focused local regressions and the actual Darwin
synthetic qualification passed; Linux x64/arm64 native execution remains
separate from that local result. These primitives do not supply encrypted-storage/key custody, enable
producer writers or close tasks 11.17/11.18.

Task 11.22 has a strict, effect-free controlled-GNOME launch specification with
exact source/dependency binding, literal private paths, bounded Unix socket
identities, isolated environment and explicit before-dispatch/readiness gates.
Source inspection confirmed that `--unlock` can create missing storage and
initialize other native slots, while failed unlock can leave the daemon alive
and invalid control paths can fall back. Restart therefore requires enforced
persisted-store write denial and actual readback, not chmod/existence or
startup-status assertions. The specification neither starts a daemon nor
authorizes input, and tasks 11.22-11.24 remain open for the native mechanisms,
production coordination and independently authorized qualification.

The Linux read-only process primitive now requires Landlock ABI >= 3 and
handles content/entry mutations with exact fresh private writable subtrees.
It preserves owned-group supervision/private stdin and explicitly excludes
metadata, IPC/received descriptors and external writers from its guarantee.
Native x64/arm64 tests are separate from portable contract/refusal tests.
Initial POSIX diagnostic run `35380872753` failed during Python
executable admission on both architectures, before native locking; the
production tool-permission checks remain unchanged while exact source-runner
tool preparation is corrected.

Run `35382671458` at `5cc876221581bfc433480af545b2121f61fa96b9`
then passed on actual Linux x64 and arm64: 27 cases passed on each host, with
only two non-Linux refusal cases unselected. Exact runner-owned executable
identity/bytes were preserved while group/other write bits were removed;
production admission remained unchanged. The run exercised native OpenTofu
contention, same-inode publication, lock-loss/stale refusal, Landlock content
and entry denial, inherited restrictions, replacement refusal and owned
cancellation. Its reports explicitly state encrypted custody was not performed.
Tasks 11.17/11.18 and 11.22-11.24 remain open for the storage/key/daemon and
end-to-end enrollment contracts rather than treating these primitives as a
completed keystore or release.

The source key-client protocol decoder now enforces bounded framing, exact
metadata, monotonic creation uncertainty and process settlement before
releasing an opaque one-use key snapshot. Private streaming observation retains
already decoded returned identities when supervision discards raw output on
timeout, without forwarding observer/provider diagnostics or key bytes.
Native client compilation, authenticated invocation, persistence and enrollment
coordination remain separate outstanding work; this decoder is not readiness
or execution authority.

The native libsecret client source and parent decoder now interoperate through
the bounded `LKC1` protocol. Compile-only run `35388541555` at
`fd5b8de3e329cfc2f02e8ca712c0edd97db485c5` built actual Linux x64/arm64 helpers
against the exact clean private libsecret source/prefix and passed 70 interface
and C framing/parser cases per host. The production helper was not executed.
Source behavior uses explicit private owner binding, prompt refusal, encrypted
session admission and non-replacing exact-item operations, with creation
uncertainty preserved. Runtime loader/dependency admission, real service
behavior, custody and enrollment/restart readiness remain separate open gates.

Private runner creation and separately approved assignment now pass current
engine evidence admission and original-custody consumer regressions. Hosted
definition, group/network and actual job runner identities remain distinct;
created-resource readback is confined to its exact reviewed collection and
returned identity, not general organization access. Staging workflow source uses
stable routing names before assignment, while execution binds actual provider
IDs separately. These are source implementation results, not live qualification
of the runner/provider/profile matrix in 15.9.

The full-check artifact custody/revalidation connection and receipt-bound full
control readback are implemented. Current-source publication, the existing macOS private-state path,
bootstrap, artifact/foundation and dev producers have bounded local admission,
execution/readback and recovery coverage. The source-task completions in this
section do not establish live GitHub/Azure or complete end-to-end activation.

The user approved explicitly representing the required organization
administration read grant in policy 8 and credential-policy schema 2, with
broader-read disclosure, fresh exact approval and historical preservation.
The amended permission contract is implemented: schema 2 requires actual
auth-kind-specific provider grants and broader-read disclosure; extra, missing
or aliased grants fail admission. Human plan/approval output shows the scope,
exact operations, digest and expiry before approval is stored. Existing-App
producer tests exercise original dispatch/run/artifact custody, pending-run
recovery without redispatch and separately approved policy finalization.
These use bounded provider fixtures, not live credentials or qualification.

Task 11.2 remains open for exact PAT bearer/lifetime proof and supported
conditional secret creation; GitHub's create-or-update operation must not become
implicit replacement. Legacy masked enrollment now stops before prompting or
writing rather than fabricating current proof from a secret-write timestamp.
Neither the planning amendment nor source acceptance authorizes credential use,
provider calls, deployment or publication.

Positive local staging/rehearsal and built-in producer acceptance now runs
through the production producers and original receipts with bounded provider
fixtures, not substituted downstream qualification readers. It covers private
DNS/TLS and build ancestry, pending dispatch, lost responses, separately approved
rollback, and exact renewed approvals. Bounded chunked rehearsal storage
preserves original records without increasing the shared 64 KiB record limit.
Task 11.15 remains open specifically for credential production/recovery blocked
by 11.2; the other tested recovery paths do not grant credential authority.

## 12. Native bundles and owner-aware upgrades

- [x] 12.1 Build a private pinned Node/ESM/runtime-dependency/resource bundle and relocatable launchers from the new resource boundary; verify metadata-only operation outside the checkout without ambient Node/npm.
- [ ] 12.2 Build and qualify macOS x64/arm64 payloads and explicit Homebrew cask definitions in the upstream-maintained tap; verify cask installation/upgrade selection, read-only resources, literal paths and exact tap/cask ownership without assuming official Homebrew acceptance.
- [ ] 12.3 Build and qualify Windows x64/arm64 portable payloads and launchers; verify complete ZIP resource closure, actual executable identity and stock supported host behavior without a user-installed language runtime.
- [ ] 12.4 Build and qualify Linux x64/arm64 direct archives with explicit host/glibc floors; verify unsupported targets fail clearly and supported installation/relocation does not depend on checkout files.
- [x] 12.5 Implement schema-1 native release/build/resource manifests and final signing/checksum/provenance verification; verify wrong product, target, source, signature and post-signing byte changes block admission.
- [ ] 12.6 Implement verified Homebrew-cask/WinGet/direct installation-owner discovery; verify same-named formulae, Homebrew-owned Node and npm-owned Liftoff cannot be mistaken for the registered native cask and unknown/linked/cache origins gain no replacement authority.
- [ ] 12.7 Implement non-installing native upgrade checks against release authority and actual owner availability; verify stale catalogs, manager lag, enterprise policy and no automatic source/configuration refresh.
- [ ] 12.8 Implement routine owner-preserving upgrades with dedicated-command authorization and exact internally bound targets; verify no new mandatory confirmation/fingerprint flag, no npm replacement and no unrelated package upgrade.
- [x] 12.9 Implement direct receipt-owned staged replacement and exact launcher activation; verify failed candidates preserve the usable current version and active payloads are not cleaned prematurely.
- [x] 12.10 Implement Windows locked-file close/handover behavior; verify no successful upgrade is reported before actual replacement/readback and no unrelated processes are terminated.
- [x] 12.11 Verify replacement by explicit path, actual owner records, resource identity and ordinary command resolution; verify a different PATH installation cannot manufacture success.
- [x] 12.12 Replace npm-latest/manual-install assumptions in doctor, compatibility remedies, help, published verification and release scripts; verify native discovery is independent of project npm configuration and historical npm behavior remains correctly labeled.
- [ ] 12.13 Package and integrity-bind the registered private-state helpers for every required native target, with declared ABI/host constraints and licenses; verify read-only relocated operation, exact helper/tool admission and sensitive-I/O closure without the build checkout, undeclared ambient runtimes or user-side helper compilation, and retain separate native-helper measurement.

Windows direct-handover implementation is covered by real file transactions,
controlled lock failures, exact-PE and receipt readback, and launcher source
tests. Identical signed PE bytes remain mapped while the verified receipt selects
the new payload; different locked bytes retain recovery and an exact owned
versioned-executable continuation. This does not qualify actual Windows image
locks, ACL/policy/controller behavior, cancellation or minimum hosts; the native
qualification tasks above and below remain open.

Local source verification now executes the confined owner-selected and
PATH-selected launchers rather than invoking the payload twice. Cryptographic
manifest/resource admission, changed bytes, wrong versions and substituted
owner/PATH records have regression coverage. Tasks 12.5/12.11 describe that
implementation, not actual approved signing or installed six-target qualification.
WinGet's required non-mutating production observation remains an implementation
blocker for 12.6-12.8, not merely a catalog-publication delay.

Task 12.12 source behavior and documentation now pass the native doctor/upgrade
lane and the compatibility, help, public-documentation and release-script
assertions. Historical npm verification remains explicitly selected and cannot
serve as native discovery, channel availability or release qualification.

## 13. Direct npm-to-native installation handover

- [x] 13.1 Implement non-mutating installation inspection usable from a verified unlinked native bundle; verify exact executable/package/prefix/PATH ownership and conflicts without creating project or notice state.
- [x] 13.2 Implement schema-1 installation-migration plans and records with explicit target owner, final package identity, ordered effects and legacy recovery identity; verify changed owner, destination, candidate or source facts reject stale plans.
- [x] 13.3 Implement the default-No interactive and exact machine authorization paths for installation-owner migration; verify routine upgrade, project migration and generic Yes grant no handover permission.
- [x] 13.4 Implement the macOS npm/Homebrew-cask launcher-conflict handover with verified unlinked candidate first; verify only the identified legacy Liftoff package is retired before conflicting native cask registration.
- [ ] 13.5 Implement Windows npm-shim/WinGet conflict handling with native path and terminal semantics; verify exact owner/order, no force overwrite and final explicit-path plus PATH resolution.
- [x] 13.6 Implement Linux historical npm/direct handover using exact receipts and launcher scope; verify unapproved existing launchers block rather than being replaced by filename.
- [x] 13.7 Implement interruption and failure recovery with actual pre-effect checkpoints; verify partial legacy removal/native installation is reported truthfully and restoration never runs blindly over another owner.
- [x] 13.8 Verify old projects are byte-identical through handover, including manifests, Node/npm dependencies, locks, framework files, history and state; verify the guide offers separate read-only assessment/update without reinitialization.
- [x] 13.9 Remove future npm publication/bridge behavior while retaining historical tarballs, provenance and explicit-version recovery tooling; verify an old npm updater reporting current is not described as native release discovery.

Installation source coverage includes exact released manifest-v2-v7 and original
activation/history/journal bytes preserved through direct handover, upgrade,
interruption, read-only recovery, and separately approved retry. This does not
qualify Homebrew or WinGet handover, final signed artifacts, or other native
hosts; those gates remain separate.

Source handover now rejects manager/direct destination conflicts before retiring
npm, including conflicts outside PATH, and binds manager destinations to exact
approval. Homebrew accepts an independently verified unlinked candidate without
confusing it with an installed cask. Interrupted manager installation supports a
freshly approved verification-only retry without another uninstall/install.
Isolated filesystem/subprocess and bounded manager fixtures cover these effects;
13.4/13.6/13.7 do not claim real owner-channel or foreign-host qualification.

## 14. Canonical skills and qualified agent-host delivery

- [x] 14.1 Author the eleven canonical workflows and capability/schema catalog from shared contracts; verify host variants do not duplicate business logic or introduce a model SDK or independent per-skill SemVer.
- [ ] 14.2 Implement Copilot, Claude Code and Codex projection adapters with a tested discovery/transport matrix; verify each declared host resolves its intended canonical workflow and preserves invocation identity.
- [x] 14.3 Implement reviewed personal skill delivery before project initialization, including one shared qualified Copilot/Codex projection; verify no project/host-settings rewrite and disclose other-host visibility where roots overlap.
- [x] 14.4 Implement exact skill ownership records, collision detection, guarded update/removal and shared-consumer accounting; verify unowned, modified, framework and neighboring files remain protected.
- [x] 14.5 Preserve existing setup/governance-assessment/repair artifacts and provide only explicit compatible project transport migrations; verify new CLI installation does not automatically rewrite them.
- [x] 14.6 Implement capability negotiation and context-preserving actions in every canonical skill; verify old CLI mismatches, missing executors, unqualified hosts and wrong report schemas stop rather than trigger direct edits.
- [ ] 14.7 Qualify assessment/adoption/update/repair assistance against staged business-behavior fixtures; verify model assumptions stay advisory and all executable effects pass the same CLI admission and approval gates.
- [ ] 14.8 Qualify local-only, repository-only and full-activation setup guidance with actual outcomes; verify no silent change of unscoped activation, fake production proof, generic Yes or manual hash-copy requirement.

Personal delivery and ownership were exercised through the public CLI in isolated
homes, including exact approval, shared consumers, no-op repetition, interruption,
and attributable recovery. This is local implementation evidence, not completion
of the separate native-host or live-journey qualification tasks above.

Discovery now also checks alternate roots and unselected hosts that share the
same physical projection. Plans bind those observations, including absence, so
late conflicting copies cannot inherit old approval. Local source regressions
do not qualify actual Copilot, Claude Code or Codex discovery and journeys.

## 15. Integrated correctness, coverage and production qualification

- [x] 15.1 Add version-matched coverage providers/configuration for the CLI and telemetry packages, including unimported production TS/JS; verify each package produces independent usable counts for all four metrics.
- [x] 15.2 Enforce strict raw-count acceptance `covered * 100 > total * 80` for every required metric; verify equality, rounded-above-80 display, missing/invalid/zero reports and combined-package masking cases.
- [ ] 15.3 Inventory and appropriately instrument/qualify native helper code separately from V8 metrics; verify unmeasured helper scope is disclosed and cannot disappear from a complete-qualification claim.
- [x] 15.4 Add missing behavioral tests until both packages exceed every required threshold without scope manipulation; verify exclusions contain no production behavior moved or omitted solely to inflate coverage.
- [x] 15.5 Run actual generated-app Scalar/schema route matrices for all affected backends and representative proxies; verify JSON media type, query preservation and canonical paths/components rather than HTTP status alone.
- [x] 15.6 Run the cited Checkov Azure controls and backend-disabled OpenTofu qualification on generated and supported remediated output; verify no hidden state/cloud effects and no manual post-generation edits are needed.
- [ ] 15.7 Run installed-artifact smoke and migration/recovery matrices on every declared native target, including Windows paths, no ambient Node/npm, read-only resources, owner conflicts and manager lag; verify evidence binds final signed bytes.
- [ ] 15.8 After specific external authorization, execute disposable GitHub repository-only qualification with controlled positive/negative checks and idempotent readback; verify partial failure preserves protections and no production tag/release is created.
- [ ] 15.9 After specific external authorization, execute disposable full Azure/GitHub profile qualification for each required producer/host/recipe combination; verify real effects, sensitive-state protection, bounded costs, current readback and recovery.
- [ ] 15.10 Verify the exact #78 sequence and affected historical recovery end to end: local completion, approved commit/push, later Azure inputs, reviewed revalidation and safe continuation; verify no repeated publication or rewritten original receipts.
- [ ] 15.11 Verify #79 repository completion while full activation remains blocked, followed by separately approved real qualification and hold replacement; verify old/default activation callers cannot receive narrower success.
- [ ] 15.12 Run build/type/import-boundary checks and the full supported cross-platform suite after integration; verify every advertised required capability has matching implementation, failure/recovery and qualification evidence.

Current local full-source measurement covers all 690 CLI production files and
all four telemetry files. Every metric exceeds the strict raw-count threshold
independently; CLI branches are 45,443/56,372 and telemetry branches 128/150.
The amended-contract full run with pinned uv 0.12.7 and Checkov 3.2.495 had
8,016 passing cases, 28 outdated fixture/snapshot expectations and 37 skipped
cases. The expectations were updated for the explicitly approved identity and
registered successor, and all 152 cases in the four affected files then passed.
No production exclusion, assertion removal, timeout increase or version
requirement relaxation was used. This is a full-source measurement plus focused
successful reruns, not a claim of one all-green full run. Native helpers, other
hosts, signed artifacts and live provider/dashboard journeys remain unqualified
by these V8 measurements.

Task 15.3 now also has separate Go statement instrumentation for the Windows
launcher source fixtures, wired into all three source CI hosts with retained
per-host coverprofiles. The local source run measures 62.2% of statements; it
does not run a signed PE or measure the PowerShell/POSIX helpers. Native helper
execution, cancellation, settlement and recovery qualification remain open.

Task 15.3 now also inventories eight helper identities, including embedded
Python bodies and the tool-inspection probe, separately from their V8-measured
TypeScript wrappers. The authentic release path binds applicable source,
compiled-module, export and program identities to final artifacts and native/
minimum-host evidence without executing archive code. Missing helper bindings
and unauthenticated assertions still fail. Required actual helper/custody and
signed installed-artifact qualification remain open; the inventory change does
not claim that those outcomes were performed.

The separately enabled local darwin-arm64 lane also passes five real Node/Vue,
Go, fresh-private-cache-negative and backend-disabled OpenTofu cases, preserving
the fixture applications and their business behavior. This does not qualify
Windows/Linux, the separate Python-repair/tool-host lanes, or any provider
deployment.

## 16. Telemetry, documentation and source hygiene

- [x] 16.1 Extend only the explicit eligible telemetry-command allowlist in client and gateway while preserving the five-field contract; verify new command validity, sensitive-field rejection, opt-outs, subprocess suppression and excluded assessment/inspection behavior.
- [x] 16.2 Preserve disclosure without notice-state writes during read-only checks and diagnostics; verify JSON stdout remains pure and no installation, model, path, profile, plan or receipt identity is collected.
- [x] 16.3 Prepare backward-compatible telemetry gateway rollout if new eligible commands require it; verify client/gateway qualification agrees and no production deployment occurs without its separate operator authorization.
- [x] 16.4 Create the explicit tracked/generated README inventory and update every entry plus directly inconsistent guides; verify root, infrastructure/bootstrap, telemetry/Grafana access, state-migration and generated application/functions/prompts/governance guidance are covered.
- [x] 16.5 Correct released/unreleased wording and identity tables from the authoritative code, document native-only cutover and separate project migration, and align the historical-only npm spec Purpose during approved spec synchronization; verify no current npm onboarding or fabricated version/hash remains.
- [ ] 16.6 Validate current command examples, native shell quoting, installed local links, resource packaging and actual capability claims across hosts/profiles; verify examples never bypass application, installation or cloud authority.
- [x] 16.7 Inventory live branches, PRs, worktrees and unique/dirty work and present an exact preservation/cleanup plan; verify only main/develop are permanent while active temporary branches remain permitted.
- [x] 16.8 Only after explicit user approval and release by owning sessions/users, prune verified stale refs and remove eligible explicitly listed branches/worktrees; verify main/develop, changed refs, unmerged commits, dirty work and active PRs remain preserved.

Branch cleanup is a verified no-op: the live inventory contains only `main` and
`develop`, both checked out, with no open PRs or stale tracking refs. No branch,
worktree, or ref was removed. `assets/qualification/source-preservation.json`
records the preservation plan, not future deletion authority.

## 17. Azure Monitor Grafana telemetry dashboard

- [x] 17.1 Resolve the existing telemetry workspace/table and operator deployment bindings from approved configuration and OpenTofu outputs; verify public dashboard source contains no hard-coded observed subscription/customer GUID or credential and no duplicate ingestion store is proposed.
- [ ] 17.2 Add the canonical version-controlled dashboard model and KQL with stable dashboard/resource identity for Azure Monitor's built-in Grafana host; verify the actual supported export/import schema and deterministic source rendering rather than assuming an arbitrary Grafana version.
- [ ] 17.3 Implement the six recorded-event, trend, command, version, nonzero-exit and latest-matching-event panels; verify their values and labels against controlled query results without unique-user, installation-count, crash-rate or outage claims.
- [x] 17.4 Implement consistent time/command/version filtering, seven-day defaults and manual refresh with bounded queries; verify interpolation, timezone/refresh context, retention limits and zero-denominator/no-match behavior.
- [ ] 17.5 Implement distinct loading, empty, filtered-no-data, access-denied, missing-table, query/throttling-failure and stale-display states using native accessible panels; verify readable normal/narrow layouts and no zero/healthy fallback after observation failure.
- [x] 17.6 Define current-user dashboard/data-access requirements and any separately approved minimal RBAC changes; verify dashboard sharing cannot bypass workspace query permission and no shared secret, ingestion identity reuse or automatic broad role grant is introduced.
- [ ] 17.7 Add the exact dashboard resource to the operator OpenTofu/AzAPI deployment boundary with a pinned supported API and resource/portal outputs; verify backend-disabled validation and a reviewed plan contain no dedicated Managed Grafana service or unintended ingestion, retention or state changes.
- [ ] 17.8 Add dashboard/KQL/provisioning contract tests for valid, sparse, empty, malformed and denied/error fixtures; verify only the existing telemetry columns are used and no synthetic usage is injected into production to satisfy tests.
- [ ] 17.9 Only after explicit operator approval, deploy and open the dashboard in the selected Azure Monitor host; verify actual rendered filters and aggregates match equivalent bounded KQL and viewer permissions behave as declared.
- [ ] 17.10 Qualify repeat no-op provisioning, portal/source drift detection and separately approved dashboard-only rollback/removal; verify the workspace, table, DCR, gateway, protected resource group and telemetry history remain intact.
- [x] 17.11 Document operator navigation, configured bindings, access, query/no-data troubleshooting, costs and the anonymous best-effort/nonzero-exit interpretation; verify no alerts, scheduled reports, new identifiers or paid hosting fallback is promised.

Task 17.4 is source implementation completion: bounded query/filter contracts,
JSON-literal interpolation, seven-day/manual-refresh/browser-timezone defaults,
query-observation timestamps and empty/denominator handling have local contract
and isolated OpenTofu rendering coverage. Actual Azure-host KQL values, data
source identity, export/import schema, error/layout behavior and deployment
readback remain unqualified under the other section-17 tasks. Structural source
success is not an operator deployment or production dashboard result.

## 18. One coordinated publication gate

- [ ] 18.1 Finalize approved native publisher/tap/cask/package identities and signing configuration through secure maintainer channels; verify manifests contain public identities only and no secret enters source, logs or planning records.
- [x] 18.2 Gate publication on the same immutable source and final artifacts/definitions across build, coverage, platform, compatibility, skills, documentation, production-capability and telemetry-dashboard evidence; verify an earlier commit, unsigned rebuild, missing platform, unavailable executor or unqualified dashboard cannot pass.
- [ ] 18.3 Stage immutable signed artifacts and submit owner-channel metadata only under explicit publication authorization; verify all required Homebrew cask, WinGet and Linux targets are independently available before advancing the stable channel.
- [ ] 18.4 Verify release/tag/build/manifest/installed-version and resource identities agree without a historical npm exception; verify package-manager readiness is not inferred solely from GitHub release availability.
- [ ] 18.5 Perform the approved small-installation native handover rehearsal and project-read-only follow-up using realistic legacy prefixes and skills; verify project migration remains separately reviewed and rollback guidance is owner-specific.
- [ ] 18.6 Advance the stable release and coordinated completion announcement only after all required gates pass and explicit release approval is recorded; verify no new npm package/bridge or required deferred capability is published as complete.
- [ ] 18.7 Reconcile issue #78, #79, #80, #81 and #82 acceptance evidence, Windows qualification, the telemetry dashboard and the full capability/task inventory before requesting issue closure; verify implementation readiness, release completion and actual authorized operational outcomes are reported separately.

Task 18.2 is source-gate implementation completion, not candidate qualification:
the real release entrypoint and workflow reject missing authenticated reports,
stale source/final bytes, incomplete native/helper/capability inventories,
unqualified dashboard/gateway results and unavailable owner channels. Controlled
regressions exercise the strict verifier without granting publication authority.
The actual candidate remains blocked on the independently registered operational
inputs and evidence in 18.1 and 18.3-18.7; no release or provider effect was run.
