## 1. Bootstrap archive integrity

- [x] 1.1 Generate workload-specific bootstrap capability purposes and verify every supported workload emits `## Purpose` before delta requirements.
- [x] 1.2 Validate the complete OpenSpec set after new and already-completed seed archives and verify failure/repair/retry through the public governance command path.
- [x] 1.3 Archive a generated new capability with the real OpenSpec CLI and verify its synchronized main spec has no fallback purpose and passes strict validation.
- [x] 1.4 Recheck archived capability existence and Purpose independently of prior evidence so upgraded projects cannot advance from an invalid archive.

## 2. Executable deterministic setup

- [x] 2.1 Add preview and executable `apply-next` forms to the shared generated setup integration and verify both Copilot and Claude outputs use the same contract.
- [x] 2.2 Run the exact generated executable argv in a generated-project regression and verify it writes evidence/state, marks `seed-valid` verified, and advances `nextReadyPhase`.
- [x] 2.3 Add additive governance verification fields for consistency, setup status, and completion, reject phase-forbidden terminal states, and return a complete failure envelope when inspection is indeterminate.
- [x] 2.4 Reject phase-forbidden evidence during readiness and phase-forbidden adapter results before writing evidence or authorizing descendants.

## 3. Contracts and release readiness

- [x] 3.1 Update main and delta specifications plus public and developer documentation, including the retired-alias upgrade instructions, and verify documentation contracts.
- [x] 3.2 Run strict OpenSpec validation, the complete repository check, and package smoke verification successfully.
