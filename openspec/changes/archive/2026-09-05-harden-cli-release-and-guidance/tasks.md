## 1. Release safety

- [x] 1.1 Enforce canonical Liftoff package identity and valid SemVer before release work; verify consistently renamed and malformed-version fixtures fail.
- [x] 1.2 Restrict the legacy installed-version exception to `0.3.3`; verify modern targets fail before installation and supported historical verification retains all other gates.

## 2. Helper and diagnostic correctness

- [x] 2.1 Make project infrastructure recipes use the generated module directory and first selected or explicitly valid environment; verify nested paths, spaces, prod-only projects, invalid overrides, and printed-only behavior.
- [x] 2.2 Keep default doctor freshness canonical and bound default subprocess probes; verify environment redirection cannot replace canonical authority and hung probes produce explicit bounded results.
- [x] 2.3 Narrow dependency-failure preservation wording to protected metadata and potential script changes; verify failure output no longer claims whole-scaffold rollback.
- [x] 2.4 Generate complete OpenSpec migration metadata, proposal, design, delta specs and tasks without inventing application behavior; verify a real staged migration passes strict validation and does not block bootstrap post-archive validation.
- [x] 2.5 Reject project name/cloud/region changes and enabled-to-disabled governance changes with existing activation state before update writes; verify byte preservation and explicit separate-migration/deactivation remedies.

## 3. Guidance and readiness

- [x] 3.1 Make generated infrastructure guidance environment-correct and governance-aware without changing infrastructure behavior; verify enabled/disabled governance and prod-only rendering.
- [x] 3.2 Document the audited functional engines, recommended incremental structure, and larger deferred capability gaps; verify documentation links and scope claims.
- [x] 3.3 Run focused suites, TypeScript build, repository and package checks, and include path regressions in the existing Windows/macOS/Linux release CI gate; verify the bounded changes do not mutate another project or provision resources.
