## 1. Planning gate and versioned contracts

- [x] 1.1 Review the completed `liftoff-activation-migration` delta and its source/target contracts; verify complete proposal-to-delta coverage and strict OpenSpec validation before beginning implementation.
- [x] 1.2 Define the target activation package/contract/graph/state/evidence/approval/compatibility versions from the approved design while retaining manifest 7 and policy 6; verify exact tuple acceptance/rejection and computed graph hashes without inventing future digests.
- [x] 1.3 Define the revised graph, including bootstrap workflow publication, application prerequisites, artifact readiness, scoped completion, and lifecycle boundaries; verify dependency ordering and that every effect has an explicit permitted operation.
- [x] 1.4 Add typed journey/local/activation/migration/lifecycle results, plannable versus executable states, scoped before/after bindings, and approval bundles; verify invalid cross-scope effects and ambiguous result shapes are rejected.
- [x] 1.5 Define local/stateful repair, protected state-read scopes, backend bindings, checkpoints, and recovery records with explicit path inventories; verify schema fixtures, private/public separation, and rejection of unknown neighboring paths or backends.
- [x] 1.6 Append Codex to the shared agent, artifact, and framework inventories without changing existing IDs/order; verify normalization, duplicates, defaults, and manifest/configuration round trips.
- [x] 1.7 Define causal workstation/remedy contracts without weakening pinned runtimes; verify unsupported causes cannot become successful readiness or generic PATH results.

## 2. Scoped readiness planning and progress

- [x] 2.1 Introduce one explicit local phase inventory using `seed-valid`, `seed-verified`, and `seed-archived`, retaining Spec Kit finalization semantics; verify both workflows reach local completion without commit, push, cloud credentials, or later executors.
- [x] 2.2 Partition shared integrity and local/activation/lifecycle completion while retaining migration/journey progress; verify real external activation blockers do not erase fresh local proof and local completion is not full-journey success.
- [x] 2.3 Add explicit scope parsing and output contracts, including lifecycle, while retaining default activation selection; verify invalid flags fail before effects and local-only requests cannot enter live stages.
- [x] 2.4 Separate dependency-ready plan construction from approval-satisfied execution; verify publication/activation plans are available before their approval exists and cannot execute prematurely.
- [x] 2.5 Record expected scoped before/after outputs for planned source/state transitions; verify workflow publication or cutover does not self-invalidate unrelated proof, while unplanned edits still block affected work.
- [x] 2.6 Reinspect after execution and expose real current readiness, operation IDs, and partial effects; verify no executed phase is echoed as future readiness and interrupted external work is not dispatched twice.
- [x] 2.7 Implement schema-2 selected-scope verification/exit behavior and lifecycle separation; verify complete, incomplete, inconsistent, awaiting-approval, pending-external-operation, and due-cleanup cases.

## 3. Workstation compatibility and causal remediation

- [x] 3.1 Replace ambiguous version extraction with bounded tool-aware parsing; verify stable and preview Copilot output with/without trailing periods, Codex output, Python release candidates, numeric framework prereleases, build metadata, and malformed output.
- [x] 3.2 Accept compatible official stable/preview Copilot, Claude, and Codex with preview notices; verify all selected-agent cases while retaining exact OpenSpec/Spec Kit and supported Node/npm/Python/Go/uv/OpenTofu constraints.
- [x] 3.3 Separate update availability from actual incompatibility and select only operation-relevant prerequisites; verify newer-release notices and unavailable cloud authentication do not block unrelated local completion, while missing applicable local executables remain blockers.
- [x] 3.4 Capture resolved executable identity and observed/required constraints through portable process helpers; verify Windows shims, spaces, multiple candidate launchers, missing executables, and successful version probes are classified accurately.
- [x] 3.5 Select registered remediation by failure cause and install origin rather than always using the missing-tool installer; verify supported install/upgrade/channel cases and explicit limitations when no safe compatible recipe exists.
- [x] 3.6 Correct post-install classification and no-progress handling; verify zero-exit/no-op installers retain the original cause, do not claim writes, and do not produce `restart-required` when the executable already resolves.
- [x] 3.7 Restrict restart guidance to observed executable-discovery failures and preserve independent per-tool consent; verify failures, declined consent, genuine PATH refresh, unchanged retries, and that no unapproved uninstall/downgrade or real package-manager action occurs in tests.

## 4. Repair preview and explicit authority

- [x] 4.1 Add project repair discovery/planning services around the existing safe project boundary resolver; verify explicit paths, nested invocation, ordinary non-Liftoff directories, malformed manifests, and retired workloads without fallback writes.
- [x] 4.2 Register repair check/live/sensitive-state-read/add-agent/default/project/approval/recovery grammar and independent prerequisite flags; verify conflicting scopes, missing consent, force/yes bypasses, and cross-project receipts fail before access or mutation.
- [x] 4.3 Build exact project/backend-bound preview receipts with requested local/stateful lanes, source/target observations, verification, and recovery scope; verify deterministic fingerprints, expiry, disclosure, and separate state-read versus write authority.
- [x] 4.4 Build exact before/after project diffs using explicit inventories and isolated staging, not `liftoff init` against the target; verify preview preserves target bytes, activation records, user-global tool files, and unrelated skills.
- [x] 4.5 Implement interactive/noninteractive exact-plan approval with distinct local, stateful, and prerequisite scopes; verify JSON is formatting only and a local fingerprint cannot authorize backend writes or resource changes.
- [x] 4.6 Keep independent agent work out of unrelated migration scope and implement reviewed recovery-plan selection; verify requested-scope completion, unknown/unsupported plan-only outcomes, and that old approvals cannot expand into new effects.

## 5. Metadata discovery and protected state inspection

- [x] 5.1 Separate observed `verified-undeployed`/`stateful`/`unknown` from lane eligibility; verify stateful is executable only with a supported complete plan, and missing local files or assertions alone prove neither absence nor safety.
- [x] 5.2 Derive only supported local state/resource bindings from safe configuration and recorded infrastructure identity; verify unknown expressions, external paths, unresolved environments/providers, and incomplete scope remain plan-only rather than triggering broad searches.
- [x] 5.3 Implement bounded metadata-only live discovery with exact scope and existing permissions; verify it never pulls state contents, authenticates, grants roles, initializes backends, or mutates resources.
- [x] 5.4 Implement explicit protected state-read approval and private execution workspaces; verify raw state/plans/keys never reach chat, stdout, repository history, or ordinary workflow artifact channels.
- [x] 5.5 Bind backend identity, lineage/serial/version/digest, input scope, completeness, principal/host, and expiry; verify denied, stale, ambiguous, incomplete, or unsafe observations cannot become valid write preconditions.
- [x] 5.6 Derive complete resource-instance/environment mappings through supported semantic inspection; verify every instance has one accounted-for destination or preserved source disposition and no naming guess silently assigns ownership.
- [x] 5.7 Re-observe approved preconditions immediately before effects and produce actionable blocked plans for unsupported cases; verify changed state, missing access, unsafe modules/data sources, or incomplete mappings prevent the affected operation.

## 6. Semantic infrastructure repair

- [x] 6.1 Implement an encapsulated semantic HCL inspection adapter with source-preservation data and explicit unsupported results; verify parsing/round-trip fixtures, package/runtime portability, and that no regex ownership inference or unmanaged global executable is introduced.
- [x] 6.2 Map the exact retired flat-root, shared-module, retained, and selected environment identities into candidate transformations; verify complete dev/staging/prod inventories and that unrelated filenames are not selected through patterns.
- [x] 6.3 Preserve supported resource/data bodies, variables, provider aliases, compatible pins/locks, outputs, and environment semantics while introducing shared-module consumers; verify semantic-preservation fixtures and plan-only rejection of ambiguous constructs.
- [x] 6.4 Render every required independent-root file and valid module reference, retaining applicable remote-backend examples and tfvars identities; verify no missing versions/locks/outputs and consistent helper/baseline working directories.
- [x] 6.5 Handle recognized partial migrations, including moved files and equivalent duplicated resource bodies; verify only established equivalent definitions are deduplicated and unsupported customizations produce a concrete plan without writes.
- [x] 6.6 Validate undeployed candidates with isolated backend-disabled checks and stateful candidates through their separately authorized protected planner; verify local-only scope never touches live state and migration plans reject unapproved resource mutations or provider-major upgrades.

## 7. Repair transactions, provenance, and resumable verification

- [x] 7.1 Reuse or parameterize reviewed filesystem transactions for the registered repair journal while sharing project locks and preconditions; verify overlapping update/repair transactions, symlinks/junctions, case collisions, and stale snapshots are rejected.
- [x] 7.2 Persist original manifest/provenance and immutable receipts at the appropriate local commit or verified stateful cutover boundary; verify historical retention, unchanged unrelated hashes, and accurate actual repaired inventories.
- [x] 7.3 Keep repair records outside managed-core replacement authority and distinguish immutable receipts from mutable progress; verify ordinary update, force, preview cleanup, and failed verification cannot rewrite source history.
- [x] 7.4 Implement pre-commit rollback and interrupted repair recovery using exact recorded paths; verify injected failures at preparation, staging, mutation, and commit boundaries expose partial rollback instead of silently losing files.
- [x] 7.5 Run only the repair's approved verification and obtain current proof through the versioned execution contract; verify no metadata-only evidence, unregistered scripts, or effects outside the local/stateful scope.
- [x] 7.6 Preserve committed repairs when subsequent local checks fail and preview only remaining verification on retry; verify exit-2 partial outcomes, current active provenance retention, and no duplicate transformation/history commit.
- [x] 7.7 Separate machine prerequisite outcomes from project rollback and protect sensitive intermediate files; verify private bounded journals, secret-free public receipts/output, independent install/dependency/profile consent, and no automatic global rollback.
- [x] 7.8 Preserve opt-out and historical-identity boundaries; verify repair does not enable profile `none`, invent a bootstrap bundle, retag historical evidence, or bypass the declared activation-contract successor.

## 8. Codex framework generation and additive integration repair

- [x] 8.1 Extend official framework command builders and marker inventories for Codex using the pinned OpenSpec 1.11.0 and Spec Kit 1.0.1 contracts; verify all seven nonempty agent subsets and every selected Spec Kit default.
- [x] 8.2 Make OpenSpec delivery validation agent-capability-aware so Codex receives all 12 skills under `.agents/skills` without required command files; verify Copilot/Claude still receive their supported complete surfaces.
- [x] 8.3 Add native Codex setup/assessment skills with distinct logical names and actual invocation guidance; verify full-journey versus read-only behavior, metadata/hashes, no Claude-name collisions, and no independent skill version.
- [x] 8.4 Expand staging boundaries only for explicitly inventoried `.agents` and applicable project-local `.codex` output; verify custom neighboring skills/configuration, unsafe links, and case collisions are preserved or rejected correctly.
- [x] 8.5 Isolate framework execution homes and seed only the approved public profile needed for staging; verify actual pinned framework integration runs cannot change real Codex global prompts, account settings, or unrelated user files.
- [x] 8.6 Implement additive existing-project integration planning through official framework operations, including Spec Kit `integration use` only for an explicit default change; verify existing integrations/defaults and customized shared templates are not silently replaced.
- [x] 8.7 Commit only reviewed agent/default desired-state fields with validated framework output, manifest selection, and managed wrappers; verify already-selected agents with missing markers are not false no-ops, while concurrent config edits, removal/framework-switch requests, and failed official commands leave protected target files unchanged.
- [x] 8.8 Register Codex workstation detection and supported platform remedies under the shared compatibility model; verify Codex-only projects do not require other agents and compatible preview Codex is warning-only.

## 9. Activation-contract successor and historical compatibility

- [x] 9.1 Implement strict source readers for the exact supported v1/v2 identities and the current target, following the completed migration delta; verify mixed, malformed, unversioned, and unknown future records cannot authorize execution.
- [x] 9.2 Declare exact source-to-successor lanes and update preview output with actual graph/contract identities; verify eligibility remains separate from executable compatibility and no future hash is guessed.
- [x] 9.3 Extend the local successor transaction to preserve original manifests, state, evidence, plans, and approvals before active replacement; verify byte preservation, path safety, and rollback at each pre-commit boundary.
- [x] 9.4 Establish current proof through approved revalidation/readback without retagging historical receipts or recreating existing resources; verify changed versus still-valid input/proof cases and historical approvals never become new authority.
- [x] 9.5 Make successor revalidation resumable after commit and distinguish it from infrastructure-state migration; verify partial outcomes preserve the target/history and do not trigger provider writes under update approval.
- [x] 9.6 Update source-of-truth/task-projection and all consumers for the new identity family; verify current graph acknowledgement, old-history diagnostics, corrupted links, and no fallback to historical proof.

## 10. Public planning approval and protected input

- [x] 10.1 Add activation planning inputs for exact repository, tenant/subscription, environments, execution identity/path, resource bounds, and cost ceilings; verify unresolved authoritative inputs are requested without inventing defaults or expanding scope.
- [x] 10.2 Implement public dependency-ready plan preview and external fingerprint binding before approval; verify the former approval-before-plan deadlock is absent and inspection executes no planned effects.
- [x] 10.3 Implement exact approval persistence and scoped bundles with expiry, permissions, recovery boundaries, and validated derived-output bindings; verify stale/expanded/unrelated plans and cross-phase privilege escalation are rejected.
- [x] 10.4 Implement private TTY and explicitly selected protected automation input for credential enrollment/state access; verify values never enter argv, chat, public JSON, debug logs, or generated source.
- [x] 10.5 Provide owner-controlled authentication assistance and credential references without automatic privilege expansion; verify wrong account/tenant/repository, denied access, and expired credentials produce precise non-mutating prerequisites.
- [x] 10.6 Implement explicit recovery authorization and cancellation boundaries; verify approved compensation is not gated by failed release checks, while new destructive or broader effects require new authority.

## 11. Repository publication and complete Phase 0

- [x] 11.1 Plan and implement repository binding/creation only for an explicitly approved destination; verify existing repositories, no-remote projects, wrong hosts/owners, and remote identity changes without guessing or replacing remotes.
- [x] 11.2 Implement reviewed commit/push/branch and workflow publication primitives preserving existing history and unrelated files; verify actual target refs, no force/reset/rebase, concurrent edits, and protected-branch failures.
- [x] 11.3 Complete local Phase 0 workload/source/build/test/health classification from real supported inputs; verify no hardcoded capability success and no unintended project-script execution during inspection.
- [x] 11.4 Implement scoped GitHub Phase 0 observation for exact branches, contexts, workflows, rulesets, releases, environments, security/licensing, runner, and identity facts; verify denied/incomplete reads remain unknown.
- [x] 11.5 Implement scoped Azure/state/network/identity/provider/monitoring/cost ownership discovery; verify actual backend/private-path applicability and that absent DAST does not bypass unrelated provider/state requirements.
- [x] 11.6 Create or reconcile the selected framework's governance change only after its approved Phase 0 plan; verify OpenSpec/Spec Kit ownership, real baseline bindings, ambiguous active changes, and no fabricated tasks/proof.

## 12. Bootstrap workflows credentials providers and private execution

- [x] 12.1 Generate/publish the explicit minimal bootstrap verification workflows before credential-use proof; verify they can run on the appropriate initial host without depending on a not-yet-created private runner.
- [x] 12.2 Implement the approved GitHub App preference and fine-grained fallback enrollment through protected channels; verify exact repository/permission/expiry/workflow policy, secure provider writes, and no secret values in artifacts.
- [x] 12.3 Verify actual credential/API use and configured workflow consumption before credential readiness; verify secret-name/file-only and wrong-token/wrong-scope cases remain unverified.
- [x] 12.4 Implement provider namespace derivation, registration policy, bounded terminal readiness, and readback for all approved resource types; verify idempotence, denied/transitional states, and that rollback never unregisters shared providers.
- [x] 12.5 Implement backend/execution-path selection from actual requirements and verified access; verify existing suitable paths are reused and unknown/private reachability cannot fall back to public access or ordinary local state.
- [x] 12.6 Implement approved minimum access-establishing bootstrap resources and protected local state when required; verify exact cost/permission/ownership scope and no unrelated application provisioning.
- [x] 12.7 Implement GitHub/Azure runner-network, group, repository/workflow assignment, egress, DNS, and actual target/backend reachability producers; verify no duplicate assignments, unsupported regions/quotas, partial readiness, or unproven isolation.
- [x] 12.8 Implement verified remote import/readiness for bootstrap resources using current mappings, locking/versioning, and no-change proof; verify retention starts only after complete remote parity and retained local state cannot be reused.

## 13. Application prerequisites workflow source artifact and deployment

- [x] 13.1 Implement the approved registry/identity/federation and supporting prerequisites stage; verify least-privilege scope, real ownership/readback, constrained derived IDs, and no unowned-resource adoption.
- [x] 13.2 Generate registered workload-aware workflow/ruleset/documentation sources while preserving customizations; verify pinned actions, exact contexts/ref families, existing-control reuse, and no duplicate or placeholder security jobs.
- [x] 13.3 Publish approved source and dispatch the actual artifact build through bounded GitHub operations; verify source/workflow/run identity, no repeated dispatch after interruption, and no unapproved branch or workflow changes.
- [x] 13.4 Build/publish and verify the real immutable application artifact and required provenance; verify mutable tags, placeholder images, missing digests, and mismatched source/run receipts cannot satisfy readiness.
- [x] 13.5 Apply only the saved approved application infrastructure plan with verified identity/backend/artifact prerequisites; verify plan/input drift, cost/permission expansion, partial provider failures, and independent resource readback.
- [x] 13.6 Verify required workload health, environment bindings, and monitoring/alert integration for the supported profile; verify shallow/unobservable/custom unsupported behavior is not reported as completed policy coverage.

## 14. Qualification enforcement and live verification

- [x] 14.1 Implement actual development and applicable staging qualification producers bound to source/artifact/environment/run identity; verify skipped, cancelled, neutral, stale, and synthetic successes fail the required gate.
- [x] 14.2 Implement approved production promotion and rollback rehearsal without adding prohibited manual workflow reviewers; verify actual promotion/rollback evidence and preserved emergency rollback access.
- [x] 14.3 Produce controlled green/red proof for exact required contexts and applicable develop/main/release/hotfix families; verify harmless isolated negative probes actually fail the intended control and cannot deploy a violation.
- [x] 14.4 Implement final enforcement planning and separate approval after current qualification; verify an earlier infrastructure approval cannot authorize ruleset enforcement or a changed rule payload.
- [x] 14.5 Implement repository-scoped idempotent ruleset application and independent source-matching readback; verify existing matches, partial writes, wrong repositories, changed contexts, and non-destructive repair/recovery.
- [x] 14.6 Compute current activation completion only from mandatory applicable live proof; verify local files, accepted submissions, expired observations, omitted mandatory environments, and incomplete controls cannot produce full success.

## 15. Supported stateful migration execution

- [x] 15.1 Register and qualify supported local/Azure Blob recipe combinations for address refactoring, backend relocation, and environment partitioning; verify unsupported backends/encryption/constructs remain blocked rather than silently downgraded.
- [x] 15.2 Create and verify protected source/destination backups and stateful working journals outside repository history; verify encryption/access controls, recovery references, bounded storage, and no state-transfer use of ordinary workflow artifacts or repository secrets.
- [x] 15.3 Implement native/backend writer coordination, locking, conditional version checks, and ownership of held locks; verify lease loss, competing writers, and stale lineage/serial/version/digest prevent unsafe writes without force bypass.
- [x] 15.4 Implement supported same-backend address/module refactoring through native state/plan semantics; verify unchanged live resource identity and no unapproved create/update/delete/replace effects.
- [x] 15.5 Implement supported backend relocation with exact source/destination identity and protection checks; verify existing-destination conflicts, inaccessible private paths, encryption compatibility, and retained recovery material.
- [x] 15.6 Implement explicitly mapped shared-state partitioning through protected transformations and backend-aware publication; verify every instance is accounted for once, remote operations do not misuse local-only flags, and partial writes remain recoverable.
- [x] 15.7 Verify destinations before source retirement and coordinate known-writer/configuration cutover; verify no dual-active/unaccounted ownership, incomplete source disposition, or premature manifest/provenance publication.
- [x] 15.8 Run the required current post-migration mapping/backend/resource and no-change verification; verify success cannot be asserted from a journal checkpoint or provider write acknowledgement alone.
- [x] 15.9 Exercise failures after each external checkpoint and implement validated compensation or forward recovery; verify old snapshots never overwrite newer state and no atomic cross-backend rollback is claimed.

## 16. Recovery lifecycle and bounded operation control

- [x] 16.1 Persist bounded long-running provider/workflow operations with stable IDs and resumable polling; verify timeouts/cancellation report actual pending effects instead of hanging or duplicating work.
- [x] 16.2 Implement public recovery planning/execution from current resource/state observations; verify exact preauthorized compensation, new-authority boundaries, and preservation of unrelated resources and project edits.
- [x] 16.3 Separate activation verification from future retention/disposal progress; verify local and activation milestones do not wait 30 days and lifecycle work is never falsely marked done.
- [x] 16.4 Implement due disposal of exact approved encrypted copies/keys with current access/ownership checks and payload-free receipts; verify not-before deadlines, wrong hosts, inaccessible keys, and unlisted paths cannot be bypassed.
- [x] 16.5 Provide the supported lifecycle invocation/scheduling handoff and overdue reporting; verify read-only commands never delete and an unavailable execution host remains an explicit operational prerequisite.
- [x] 16.6 Add lifecycle handling for applicable credential expiry/rotation and retained-state non-use rules; verify current requirements become explicit notices/blockers without exposing secrets or retroactively editing historical success.

## 17. Shared diagnostics and native end-to-end orchestration

- [x] 17.1 Use one active-layout-aware managed-context builder across update, assessment, doctor, and repair; verify legacy/unknown/repaired contexts agree while genuine managed-byte conflicts remain visible.
- [x] 17.2 Report current inventory/history, CLI identity, migration/activation checkpoints, approval readiness, and lifecycle distinctly; verify source labels or old successful records cannot replace current observations.
- [x] 17.3 Emit supported exact project/backend-bound next actions for planning, approval, enrollment, execution, recovery, and lifecycle; verify no manual metadata/state-edit or misleading temporary-init recipes are generated.
- [x] 17.4 Update all three native setup integrations to drive the requested full journey across separately approved scopes and preserve local-only/declined-stage behavior; verify partial exits, required migration branches, and no-progress handling.
- [x] 17.5 Keep assessment integrations read-only and integrate current versioned activation/state-migration evidence appropriately; verify state-only proof cannot satisfy unrelated enforcement and overall alignment still requires complete applicable coverage.
- [x] 17.6 Render truthful human/JSON progress without fictitious PATH fixes, installer writes, deployment success, or rollback claims; verify snapshots for approval waits, external waits, partial cutovers, stale proof, and overdue lifecycle.

## 18. Documentation package contracts and qualification

- [x] 18.1 Update README and operator/developer guides for end-to-end setup, local-only operation, supported migration recipes, secure consent, and real external prerequisites; verify documentation/link checks and exact command examples.
- [x] 18.2 Document Codex native invocation and unchanged preview-agent/runtime policy across both frameworks; verify generated guidance agrees with the registered agent, command, and artifact inventories.
- [x] 18.3 Update logical-name, graph, manifest, proof, approval, compatibility, help, and lifecycle fixtures for the new execution family; verify old source bytes remain readable historically and are never retagged.
- [x] 18.4 Extend installed-package smoke coverage for planning/approval/enrollment/scopes/repair/recovery/lifecycle and every agent selection; verify the published entrypoint and direct source expose the same command surface.
- [x] 18.5 Add a full local-to-activation fixture journey through real producer contracts with deterministic transports; verify no unapproved effect occurs and activation requires the actual expected proof chain.
- [x] 18.6 Add stateful migration, concurrent-writer, cancellation, and partial-failure journeys; verify verified cutover, protected recovery, no duplicate operations, and no unsafe forced restore.
- [x] 18.7 Run Windows CI for spaces, drive/UNC boundaries, junctions, case collisions, shims, protected workspaces, and interrupted local/stateful operations; verify equivalent logical outcomes on macOS/Linux.
- [x] 18.8 Maintain sanitized first-party/provider contract fixtures and negative authentication/quota/ownership cases; verify tests never require a downstream application workspace or expose sensitive state.
- [x] 18.9 Add an explicitly opt-in live qualification lane using owner-approved disposable Azure/GitHub resources with bounded cost and cleanup scope; verify real publication, deployment, qualification, enforcement, and recovery without treating skipped runs as success.
- [x] 18.10 Qualify advertised stateful recipe/backend combinations against approved disposable state and resources; verify exact mapping/no-change/cutover/recovery behavior and leave unqualified combinations non-executable.
- [x] 18.11 Run targeted suites during development, then repository build/check, package smoke, and release-identity gates; verify graph hashes, declared producer coverage, privacy boundaries, and all changed contracts before release.
- [x] 18.12 Audit final scope and evidence against the completed proposal/spec set; verify no required supported producer remains a placeholder, no missing delta is ignored, and no live qualification or implementation task is marked complete without evidence.
