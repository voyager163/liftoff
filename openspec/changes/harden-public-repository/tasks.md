## 1. Refresh the implementation baseline and authority boundaries

- [x] 1.1 Before implementation, record the current approved source/ref, compare it with planning base `70d10881b46d873118d825735696f39b6d35ebe0`, and confirm implementation authorization separately from hosted/CI/publication effects; verify the delivered scope record preserves public participation, GPL terms, npm distribution and the no-cloud boundary.
- [x] 1.2 Refresh repository/branch/ruleset/Actions/environment/security-feature/readiness metadata using read-only access without secret findings; verify a dated before-state and capability matrix distinguishes available, configured, executed, passed, enforced, unavailable and blocked controls.
- [x] 1.3 Reinspect actual release/scanner inventories and any newly merged modernization specs; verify the four existing npm graph IDs, Python standard/GenAI locks, Go inputs, source IaC and telemetry/generated images are accounted for and native work is deferred unless independently merged and explicitly reconciled.
- [x] 1.4 Investigate exact-base Windows run `34842047858` and freshness run `34860731015` against current evidence, create a narrowly scoped remediation dependency if still needed, and verify no protection is removed, synthetic status posted or former candidate code imported to bypass either blocker.
- [x] 1.5 Refresh the scenario-to-evidence map for the approved normal-versus-policy-maintenance admission amendment, retaining prior evidence and adding exact-data eligibility, adopted-base authority, safe waiver retirement, drift and release-rejection cases; verify every current scenario maps to deterministic tests, real results or hosted proof/readback and that required versus reporting roles do not invent observed check names or recreate the adoption cycle.
- [x] 1.6 Inventory declared current-source/history refs and available native scan-completion/bypass metadata without fetching secret-bearing alert responses; verify a dated scope/capability record identifies exact revisions, inaccessible or unsupported surfaces, and any demonstrated gap justifying the approved local Gitleaks path rather than treating enabled settings as scan evidence.
- [x] 1.7 Refresh the public documentation, template/inheritance, reporting-route and packed-file inventories against the actual source baseline; verify a gap record reconciles README length rules, first-use/contributor paths, existing release/privacy guidance and checkout-versus-tarball navigation without inferring quality from a community score.
- [x] 1.8 Obtain the owner's private conduct destination, explicit publication consent and confirmation that the route is usable and monitored; verify the recorded decision keeps security and conduct separate, introduces no promised SLA or fictitious responder, and leaves affected policy work blocked if the input is absent without sending reports or provisioning a channel.

## 2. Establish inventories result contracts and policy invariants

- [x] 2.1 After section 1, create/reuse one named source/dependency/generated/IaC/image/packed-asset security inventory referencing existing supported-stack and generator lists; verify missing/unknown applicable entries and empty inventories fail while retired Power Apps fixtures stay non-generative.
- [x] 2.2 Map the existing seven generated-container cases plus telemetry to language, lock/extra, IaC and image coverage, adding explicit representatives for uncovered security-relevant generator branches; verify every supported distinction maps to a tested representative rather than a hand-written substitute.
- [x] 2.3 Define typed bounded report/evidence records with tool/config/database identity, policy/inventory digest, exact source/ref/event/run/attempt and artifact/platform digest; verify parser fixtures reject unknown schemas, bad paths, duplicate identities, malformed severity, mismatched source and missing output.
- [x] 2.4 Define high/critical thresholds and exact blocking rule IDs for non-comparable policy diagnostics while retaining stricter npm behavior and the independent secrets-disposition policy; verify fixtures distinguish security findings, lower-severity owner-bound triage, blocking untriaged or unremediated secret detections, and tool/infrastructure failure.
- [x] 2.5 Reuse the npm exception discipline for narrowly scoped non-npm vulnerability/policy findings without reusing npm entries, and verify exact graph/case/artifact/advisory/rule matching, owner/rationale/mitigation, 30/90-day maxima, expiry and stale/changed chains through controlled-date tests; verify those exception windows cannot waive credential exposure or replace exact secret-disposition records.
- [x] 2.6 Implement the explicit control-plane inventory, trusted-base comparison and distinct normal/maintenance admission contract; verify exact registered data-only proposals for observed base findings can be admitted with honest blocked finding reports, while mixed source/workflow/rule edits, new findings, unsupported paths/types, incomplete analysis and confirmed unremediated exposures fail eligibility without a new approval gate.
- [x] 2.7 Implement portable path resolution and registered temporary file/image lifecycle for generated scanning; verify Windows spaces, case aliases, traversal, symlink/junction escapes and cleanup failures are covered without glob-based deletion or changes outside registered scope.
- [x] 2.8 Define bounded sanitized secrets-coverage/finding records and the scanner-output redaction boundary after scope discovery; verify sentinel fixtures cannot expose raw matches through stdout, stderr, report serialization, parser errors, public artifacts or comments and that incomplete evidence remains explicitly unqualified.
- [x] 2.9 Register secret-detector rules/exclusions and exact dispositions as trusted control-plane inputs, deriving active authority from independently loaded adopted base policy rather than candidate approval fields; verify exact base-finding proposals remain nonauthoritative before merge, forged/stale provenance and exposure downgrades fail, incident records persist, and safe permission-only withdrawals cannot create grants or weaken vulnerability exceptions.

## 3. Harden unprivileged Actions execution

- [x] 3.1 After policy contracts exist, update existing and new workflow dependencies to an explicit selected-action/full-SHA policy with applicable reusable/local/nested dependency checks; verify current pinned actions remain valid and mutable or unapproved references fail focused workflow tests.
- [x] 3.2 Retain read-only defaults/no bot approval, disable unnecessary persisted checkout credentials, and scope exceptional permissions per job; verify fixture tests reject validation OIDC/content-write/cloud access and permit only explicitly needed reporting/publication permissions.
- [x] 3.3 Define fork/Dependabot execution on ephemeral hosted runners, preserve `first_time_contributors` approval, and prohibit privileged untrusted checkout/artifact execution; verify workflow/event fixtures cover `pull_request`, `pull_request_target`, `workflow_run`, arbitrary manual refs and unavailable report-upload rights without privileged fallbacks.
- [x] 3.4 Add finite job/subprocess bounds, superseded-PR cancellation, non-cancelling publication serialization and trust-separated caches/artifacts; verify timeout, cancellation, cross-domain cache and wrong-origin artifact tests never yield a passing required gate.

## 4. Extend dependency security without duplicating existing graphs

- [x] 4.1 Reuse `scripts/audit-template-dependencies.mjs`, `scripts/template-dependency-security.mjs` and `security/template-dependency-exceptions.json` in dedicated PR and release lanes while retaining weekly/manual audit; verify all four named graphs use canonical npm and manifests/locks remain byte-identical.
- [x] 4.2 Add Dependency Review for supported PR diffs with explicit limits, alongside complete-current-graph assessment; verify an unchanged vulnerable dependency is caught by the full audit and graph recognition or a clean diff cannot satisfy it.
- [x] 4.3 Pin and integrate OSV-Scanner only for the justified Python/Go full-graph gap, reconcile extracted components with both `uv.lock` graphs/extras and exact resolved Go modules, and materialize supported exact inputs when parsing is incomplete; verify real component coverage and a benign vulnerable transitive fixture rather than reachability-only or manifest-only success.
- [x] 4.4 Extend Dependabot to supported Python `uv` and Go entries after verifying ecosystem behavior, retaining the four npm directories, Actions, default-branch targeting and supported-stack ownership; verify configuration tests and delivered capability notes cover unsupported update surfaces without exempting their scans.
- [x] 4.5 Preserve deterministic ordinary audit tests and strict lower-severity npm exceptions; verify moderate unreviewed findings, stale/expired entries, wrong graph/chain and 30/90-day boundaries fail with controlled fixtures while valid exact entries report reviewed rather than fixed.

## 5. Implement source and materialized-code analysis

- [x] 5.1 After inventories and Actions boundaries exist, add advanced CodeQL for actual source and Actions with explicit supported language/category/query identities; verify a real permitted run has expected non-empty extraction and successful analysis for every applicable category, not a silently shortened matrix.
- [x] 5.2 Implement a separate source finding evaluator for complete results, including findings outside PR diff locations; verify a successful analysis with a benign high/critical fixture still fails the finding gate and missing/malformed SARIF produces an infrastructure error.
- [x] 5.3 Materialize generated projects through existing planner/generator/artifact APIs in secret-free temporary workspaces with frozen metadata; verify actual generated Node/Python/Go/frontend inputs and relevant optional branches match the named inventory without setup/deployment/cloud effects.
- [x] 5.4 Analyze materialized code with appropriate CodeQL language support and add an explicit generated-output finding gate independent of GitHub diff annotations; verify a generated-only insecure fixture turns the exact gate red and missing language/report/source counts cannot pass.
- [x] 5.5 Implement minimal-permission source reporting and native merge-protection configuration inputs without activating settings; verify fork/Dependabot event behavior, documented default-setup/diff limitations and local finding enforcement remain coherent if upload visibility is limited.
- [x] 5.6 After sections 1-3 establish the scope, gap and safe-output contract, integrate native secrets metadata and pinned local Gitleaks only for the demonstrated coverage/enforcement gap; verify current-tree, complete declared reachable history and introduced-PR-commit selection in registered isolated workspaces, including Windows path equivalence, without scanning developer homes, uploading matches or disabling native protection.
- [x] 5.7 Implement the unprivileged secrets finding result and its normal/maintenance admission bindings using trusted detector policy and sanitized dispositions; verify added-then-removed PR fixtures, shallow/missing history, unknown refs, scan errors, skipped jobs and tampering block, while exact eligible base-only false-positive proposals never clear findings before adoption or admit confirmed unremediated exposures.
- [x] 5.8 Implement sanitized credential-owner remediation evidence handling without performing credential actions; verify unresolved detections block regardless of severity, file/alert deletion alone cannot establish revocation, and exact invalidation plus current-source removal can resolve a historical occurrence without rewriting history or contacting the issuer.

## 6. Execute real IaC and image security checks

- [x] 6.1 Integrate pinned Checkov execution over inventoried committed bootstrap/telemetry and generated OpenTofu/build configuration without cloud credentials or apply operations; verify real clean and benign blocking-rule fixtures, non-empty report scope and parser/error handling rather than mocked invocation alone.
- [x] 6.2 Extend existing generated-container and telemetry build/smoke mechanisms to invoke pinned Trivy on actual built image digests and declared platforms, retaining context-exclusion checks; verify OS/application coverage, digest mismatch rejection and a safe blocking fixture while `trivy config` remains disabled.
- [x] 6.3 Bind IaC/image results into the explicit generated/source gates with registered cleanup and full expected-case checks; verify failed builds, absent images, unreadable reports, wrong platforms and cleanup errors fail and no native executable receives a pretend container verdict.

## 7. Extend the existing npm release qualification boundary

- [ ] 7.1 After security producer contracts exist, separate qualification from privileged publication within `.github/workflows/release.yml`, preserving existing identity/package/template/dist-tag/canonical checks; verify workflow tests reject arbitrary refs, PR artifacts and publication authority in dry-run jobs.
- [ ] 7.2 Pack once from the selected protected `main` commit, inspect/install/scan that tarball and publish the same bytes; verify digest substitution/repack tests fail and canonical npm verification compares integrity in addition to version, dist-tag and isolated installed behavior.
- [ ] 7.3 Produce explicit packed-component SBOM, provenance and vulnerability evidence with exact digests and 24-hour/current-attempt freshness under actually adopted policy; verify missing components, changed identities, expired evidence/exceptions, provenance without a security verdict and a maintenance-admission report presented as qualification all block publication.
- [x] 7.4 Prepare a zero-reviewer publisher environment/ref/OIDC contract and read-only feasibility record for the actual tag-creating actor; verify other job/event/ref identities cannot be assumed equivalent, and record a blocker plus separately authorized repository-only App proposal if built-in identity cannot be scoped, without installing anything or granting a PAT.
- [x] 7.5 Design creation-only `v*` publisher exception and independent no-bypass tag update/deletion policies, explicitly coordinating follow-on work despite `GITHUB_TOKEN` recursion suppression; verify policy/coordinator fixtures reject mutation by the creator, unauthorized creation and duplicate publication.
- [x] 7.6 Add complete draft-first GitHub Release assembly with explicit registered assets and immutable-state readback, keeping activation disabled until authorized; verify missing/substituted assets fail and tests distinguish future immutable assets/tags from editable notes and historical mutable releases.
- [x] 7.7 Implement precise partial-publication reporting and exact-byte idempotent retry/forward-repair handling; verify npm-success/GitHub-failure and existing-version mismatch tests never unpublish, overwrite assets, move tags or weaken controls.
- [x] 7.8 Document the native-modernization owner/interface for real platform/component/digest/SBOM/vulnerability/signing/notarization evidence without constructing native artifacts; verify npm remains the operative distribution and no test or documentation claims absent native qualification or SLSA L3.
- [x] 7.9 Bind applicable secrets coverage and disposition to the exact release source and current qualification attempt; verify incomplete scope, stale/wrong-source evidence, untriaged detections and confirmed unremediated exposures block publication despite valid provenance or dependency exceptions, with sanitized output and no automatic credential actions.

## 8. Wire recurrence aggregation and cross-platform regression coverage

- [ ] 8.1 Wire PR, protected-ref integration, weekly scans and release qualification to distinct complete-analysis, actual-finding/reporting and required security-admission roles; verify normal PRs require passing findings, maintenance eligibility preserves reported blocking findings without an unconditional-clean-check cycle, all mandatory functional/integrity checks remain required, and missing/skipped/cancelled producers fail every route.
- [ ] 8.2 Add owner-visible lower-severity triage, scheduled-failure and capability/drift reporting with sanitized evidence; verify fixtures distinguish configured/passed/enforced states and new disclosures prevent later publication without rewriting ordinary test results or history.
- [x] 8.3 Add focused deterministic fixtures for scanner/report/coverage failures, tampering and the full policy-maintenance lifecycle; verify exact base-finding proposals, expiry renewal and safe waiver retirement versus mixed edits, future grants, forged approval fields, exposure downgrades, incomplete dependency snapshots, stale head/base/policy identity and release-evidence misuse, using controlled dates and isolated synthetic repositories.
- [ ] 8.4 Run Windows, macOS and Linux CI for path/inventory/generation/report/cleanup and existing npm package behavior after authorized implementation changes; verify the same logical entries and supported commands on all OS lanes, and separately record Linux actual scanner/image platform coverage without overstating other architectures.
- [ ] 8.5 Wire initial and recurring full-scope secrets assessment plus candidate-tree/introduced-history PR assessment into the relevant required result roles; verify newly inventoried refs, changed detector policy and missing scheduled evidence cannot reuse an indefinite clean certificate or silently narrow coverage.

## 9. Qualify real checks on safe unmerged candidates

- [ ] 9.1 After sections 2-8 and authorization to run CI, exercise positive and controlled-negative fixtures on exact unmerged candidate refs and collect head/base/tested SHA, workflow/policy/tool identity, observed context/App and run/attempt results; verify every proposed required context demonstrably becomes both green and red without synthetic status posting.
- [ ] 9.2 Prove source CodeQL and generated-only Python/Node/Go findings, dependency-review versus full-graph findings, Checkov policy failures and real image findings using harmless test fixtures; verify all applicable result gates block even when analysis/upload/startup itself succeeds.
- [ ] 9.3 Exercise actual fork and Dependabot paths under existing execution approval and limited tokens; verify required checks run, no secrets/publisher/OIDC authority is available and report-upload limitations do not become skipped or privileged success paths.
- [ ] 9.4 After separately authorized hosted qualification, prove normal and exact-data maintenance admission with real observed check composition, visible pre-adoption findings, ordinary maintainer adoption and fresh reassessment; verify mixed edits, new/unknown findings, errors, expired or future grants, stale identities and release misuse block, keeping negative refs unmerged and local simulations distinct from hosted evidence.
- [ ] 9.5 Requalify the preserved functional checks, npm 10/12 template lanes, telemetry checks and exact Windows blocker after scoped remediation; verify actual successful runs and no weakened protection or modernization-only substitution before enabling new requirements.
- [ ] 9.6 Exercise release dry-run and partial-failure paths with publication/signing/deployment disabled; verify exact bytes/identity, complete draft inventory simulation, token-recursion coordination and replay rejection without creating a real release or enrolling credentials.
- [x] 9.7 After explicit scan/CI authorization, establish real declared current-source/history completion and sanitized finding/disposition evidence for the selected refs; verify expected scope against actual assessment, keep unavailable native evidence explicit, and report credential-owner remediation needs without retrieving hosted secret values or performing rotation.
- [ ] 9.8 After separate authorization for safe hosted effects, qualify native push rejection and effective secret-protection bypass behavior on disposable refs using only permitted provider-supported nonfunctional fixtures; verify actor/ref/policy identity and actual response, treat unrecognized fixtures as inconclusive, and never create live credentials, enroll actors or change access to manufacture proof.
- [ ] 9.9 Qualify actual fork and same-repository secrets intake using safe detector-supported fixtures, including added-then-removed content and exact base-only disposition maintenance; verify ordinary new-detection merge denial, distinct maintenance eligibility without resolving the finding, confirmed-exposure denial, clean-case success and redaction independently of native push behavior, with uncovered bypasses explicitly blocked.

## 10. Prepare the repository-scoped hosted migration

- [ ] 10.1 After qualified implementations merge through approved PRs, revalidate final workflow/policy identities and prepare exact desired-state payloads for `develop`/`main`; verify zero reviews, no ordinary/admin bypass, strict observed App-bound admission/analysis/integrity and existing functional checks, conversation resolution, force/deletion denial and preserved linear history without blanket update restrictions or a contradictory unconditional-clean-finding requirement on qualified maintenance.
- [ ] 10.2 Prepare selected-action/SHA policy, native CodeQL finding protection, existing native security feature preservation and zero-reviewer publisher/ref/tag/release settings using capability-verified API fields; verify schema/fixture validation rejects organization/paid assumptions, overbroad identities and unsupported fields.
- [x] 10.3 Keep source-repository desired-state data distinct from packaged governance assets and follow the documented `.github/github-app.yml` schema for any app configuration; verify no invented app fields or generated-profile activation changes enter the migration.
- [ ] 10.4 Implement read-only diff, drift/precondition checks, idempotent authorized apply and readback with scoped recovery records; verify fake-API tests preserve existing protections on partial failure and never remove controls automatically or expose secret values.
- [ ] 10.5 Deliver the exact before/after payloads, check qualification matrix, publisher feasibility evidence, historical-release caveats, capability blockers and narrow recovery plan for separate owner authorization; verify no live setting mutation is performed by preparation and unresolved identity/control gaps remain blockers.

## 11. Apply hosted controls only after separate explicit authorization

- [ ] 11.1 Obtain authorization for the exact current global repository payload and separately bounded behavioral test effects, then refresh drift and evidence; verify permission and payload identities are recorded and changed baselines stop application rather than reusing stale consent.
- [ ] 11.2 Apply and read back compatible branch/Actions/source-security controls incrementally while preserving classic protections until equivalent-or-stronger enforcement is proven; verify real check/App bindings and single-maintainer PR merges, with no extra approval gate or temporary Windows exemption.
- [ ] 11.3 Once publisher identity feasibility is proven and any required enrollment is explicitly authorized, apply exact environment/ref and distinct tag creation/mutation policies plus future release immutability; verify actor/ref reachability and readback, leaving the phase blocked rather than granting broad fallback credentials.
- [ ] 11.4 Prove denial of bypass/direct/destructive updates and unauthorized tag operations only on authorized disposable refs with equivalent payloads and publication disabled, then read back exact production targets; verify safe success paths, payload equivalence and agreed test-ref retention/cleanup without risking real branches or release tags.
- [ ] 11.5 Repeat authorized apply/readback to prove idempotence and record recovery evidence for an injected partial-application/readback failure using safe test doubles or explicitly approved test scope; verify no destructive replacement, permanent bypass or automatic rollback occurs.
- [ ] 11.6 Record final enabled-versus-executed-versus-enforced state and remaining gaps; verify public visibility/forks/issues/GPL terms, existing branches/history, npm distribution, no environment reviewers and unchanged cloud/telemetry-production scope before reporting activation.
- [ ] 11.7 Before declaring secrets protection complete, reconcile source/history scan completion, sanitized remediation state, native push qualification, fork/same-repository merge proof and effective bypass readback; verify each claimed protection has its own evidence and that unavailable proofs remain blocked without paid substitutes, additional human approval gates or weakened controls.

## 12. Deliver OSS onboarding community policies and final acceptance

After section 1, documentation and templates can be prepared alongside other implementation. Public claims must reflect actual control state, task 12.4 depends on the approved contact from 1.8, and final acceptance follows the completed documentation work and applicable qualified controls.

- [x] 12.1 Refresh `README.md` as the supported npm first-use landing page with clear terminal/agent distinctions, accessible illustration, factual status/prerequisites, privacy notice and workload/community/release links; verify fewer than 135 normalized content lines, correct command ordering and reachable safety/repair/migration guidance rather than deleted contracts.
- [x] 12.2 Reorganize `CONTRIBUTING.md` around contribution scope, public locked setup, focused checks and PRs to `develop`, moving advanced maintainer/release/registry material into appropriate linked guidance; verify actual commands, preserved anchors and safety assertions, lightweight documentation fixes, policy-dependent registry overrides and unchanged sole-maintainer/GPL responsibilities.
- [x] 12.3 Add the explicit bug/feature forms, issue chooser and PR checklist under `.github/ISSUE_TEMPLATE/` and `.github/PULL_REQUEST_TEMPLATE.md`; verify GitHub-compatible schemas, usable question/security routes, proportionate not-applicable answers, existing optional labels and warnings against credentials/private inputs without opening real issues or changing hosted features.
- [x] 12.4 After 1.8, revise `CODE_OF_CONDUCT.md` and synchronize the authoritative support/reporting map across community entry points; verify private conduct and vulnerability routes are distinct, the contact is approved and non-placeholder, practical handling limits are honest, and no corporate address, independent responder or response guarantee is invented.
- [x] 12.5 Update contributor, security, developer and release guidance to describe actual controls and the bounded policy-only maintenance path: normal merge as adoption, truthful unresolved findings, no candidate authority/future grants, safe waiver retirement and fresh release qualification; verify these distinctions alongside existing fork, exception, capability and recovery contracts without implying a clean scan or an extra approval gate.
- [x] 12.6 Document dependency graph versus diff versus update coverage, generated-output limits, scanner/platform identities, no secret-value disclosure, Autofix-as-suggestion and deferred native handoff; verify no paid/preview, full governance, native qualification or historic immutability claims are implied.
- [x] 12.7 Document supported secrets patterns and declared scan scope, the conditional Gitleaks responsibility, safe reporting and owner-authorized revocation/rotation, and local-commit versus push versus fork-merge boundaries; verify guidance matches actual evidence, explains bypass/false-negative limitations, and never claims that source deletion or enabled settings prove secrets safety.
- [x] 12.8 Extend the explicit package documentation surface and existing package-smoke assertions for README-linked CONTRIBUTING, SECURITY, CODE_OF_CONDUCT and other declared local targets; verify those exact files and their navigation from the extracted qualified tarball, with missing-artifact fixtures failing despite a complete checkout.
- [ ] 12.9 Extend existing documentation tests for the shared normalized README budget, canonical file/section links, moved anchors, routing/template consistency and safe intake prompts; verify LF/CRLF parity and platform-native path resolution on Windows/macOS/Linux, retaining rather than deleting upgrade/repair/privacy/safety assertions.
- [x] 12.10 Review public status, supported-release/prerequisite facts, telemetry defaults/opt-outs, contribution licensing, AI-assisted submission responsibilities and observable badges against their authoritative sources; verify no new CLA/DCO, corporate affiliation, support SLA, certification or completed-control claim is introduced by imitation of benchmark projects.
- [ ] 12.11 Walk the documented user and contributor paths in isolated supported environments after authorized implementation; verify install/init/help and scope-appropriate checks match actual behavior, agent instructions are distinct from terminal commands, and no cloud setup, publication, real issue/report submission or additional community service is required to demonstrate the paths.
- [ ] 12.12 After the documentation/intake work and applicable security qualification, complete the scenario-to-evidence matrix and run focused documentation/package/security regressions plus strict OpenSpec validation; verify every applicable scenario has evidence and missing conduct-contact approval or blocked hosted tasks remain explicit rather than being reported complete.
- [ ] 12.13 Hand off release/exception/drift maintenance, current documentation/reporting-route ownership and future native integration interfaces with retained evidence locations; verify a maintainer can identify outstanding findings, exact expiry dates, publisher/contact limitations and partial-release recovery without triggering publication, signing, deployment or report submission from the handoff itself.
