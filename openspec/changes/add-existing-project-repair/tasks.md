## 1. Semantic infrastructure candidate

- [x] 1.1 Add bounded semantic HCL inspection and explicit source/target inventories; verify supported legacy, customized, partial-move, missing, and ambiguous fixtures.
- [x] 1.2 Produce complete selected-environment module consumers while preserving source bodies, provider locks/constraints and values; verify parsed semantics and no unrelated replacement.
- [x] 1.3 Derive exact supported resource/backend discovery scope and reject unsafe/unknown constructs; verify missing state alone cannot yield executable eligibility.

## 2. Reviewed repair coordinator

- [x] 2.1 Register repair check/live/subscription/approve-plan/recover parsing, dispatch and schema-1 output; verify help, conflicting flags and explicit/nested project selection.
- [x] 2.2 Implement bounded metadata observations with explicit subscription and existing authentication; verify absent, present, denied, timeout, invalid output and no-live cases without real cloud access.
- [x] 2.3 Bind expiring external previews and approval to exact project/configuration/effects; verify stale edits, changed destinations, cross-project receipts and no authority expansion.
- [x] 2.4 Validate staged candidates with approved backend-disabled commands, commit exact files and actual provenance, and preserve history; verify successful transformation, validation failure, repeated repair and unrelated bytes.
- [x] 2.5 Reuse the sealed transaction for a registered repair journal and explicit recovery; verify injected interruption, rollback, concurrent changes, unsafe paths and update/repair exclusion.

## 3. Developer journey

- [x] 3.1 Replace cryptic local verification phase errors with human meaning and project-targeted repair/resume actions in human and JSON output; verify both default and explicit project paths.
- [x] 3.2 Wire native setup guidance to the supported repair journey and document public executable/plan-only boundaries; verify generated integrations and documentation commands.

## 4. Integration qualification

- [x] 4.1 Run targeted repair/update/governance regressions, TypeScript build and installed-package smoke; record actual failures and fix regressions. Final full suite: 2,718 passed, 26 opt-in/platform skips; both native repair suites separately passed all 76 cases; build, supported-stack and installed-package smoke passed.
- [ ] 4.2 Include repair scenarios in Windows CI and obtain native Windows verification for spaces, path confinement and interrupted transactions before archive; do not mark a non-run as passed. CI coverage is configured, including native backend-disabled validation. Native Windows execution remains pending; the current native run was macOS.
- [x] 4.3 Validate the OpenSpec delta and reconcile completed task markers with evidence; leave external/unrun qualification explicitly incomplete. Strict change validation passed; Windows qualification remains unchecked.
